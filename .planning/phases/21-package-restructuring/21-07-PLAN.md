---
phase: 21-package-restructuring
plan: 07
type: execute
wave: 2
depends_on: ["21-06"]
files_modified:
  - src/quantum_language/qint_comparison.pxi
  - src/quantum_language/qint_division.pxi
  - src/quantum_language/qint.pyx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "qint comparison operations (eq, lt, gt, le, ge, ne) are in separate include file"
    - "qint division operations (floordiv, mod, divmod) are in separate include file"
    - "qint.pyx uses include directives to assemble the complete class"
    - "No single Cython file exceeds ~500 lines"
    - "All tests pass with restructured qint module"
  artifacts:
    - path: "src/quantum_language/qint_comparison.pxi"
      provides: "Comparison operations for qint class"
      contains: "__eq__|__lt__|__gt__|__le__|__ge__|__ne__"
    - path: "src/quantum_language/qint_division.pxi"
      provides: "Division operations for qint class"
      contains: "__floordiv__|__mod__|__divmod__"
    - path: "src/quantum_language/qint.pyx"
      provides: "qint class definition with include directives"
      contains: "include.*qint_arithmetic|include.*qint_bitwise|include.*qint_comparison|include.*qint_division"
      max_lines: 500
  key_links:
    - from: "qint.pyx"
      to: "*.pxi files"
      via: "Cython include directive"
      pattern: "include \"qint_"
---

<objective>
Complete the qint.pyx split by extracting comparison and division operations, then refactoring qint.pyx to use include directives.

Purpose: Finish addressing verification gap - reduce qint.pyx from 2432 lines to ~400 lines using Cython include file pattern. Each resulting file will be under ~500 lines.

Output:
- qint_comparison.pxi (~470 lines)
- qint_division.pxi (~400 lines)
- Refactored qint.pyx (~400 lines) using include directives
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-package-restructuring/21-VERIFICATION.md
@.planning/phases/21-package-restructuring/21-06-SUMMARY.md
@src/quantum_language/qint.pyx
@src/quantum_language/qint.pxd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create qint_comparison.pxi</name>
  <files>src/quantum_language/qint_comparison.pxi</files>
  <action>
Extract comparison operations from qint.pyx (lines ~1567-2034) into a new include file.

The file should contain:
1. A header comment explaining this is an include file for qint class
2. `__eq__` method (equality comparison with C-level CQ_equal_width)
3. `__ne__` method (inequality via NOT equality)
4. `__lt__` method (less-than via in-place subtraction)
5. `__gt__` method (greater-than)
6. `__le__` method (less-than-or-equal)
7. `__ge__` method (greater-than-or-equal)

Important notes:
- Do NOT include any imports - those stay in qint.pyx
- Do NOT include the cdef class declaration - just the methods
- Methods will be indented at class level (one tab) when included
- Use same formatting/style as original qint.pyx
- Include all docstrings
- Note: These methods use `from quantum_language.qbool import qbool` internally - that import stays in qint.pyx
  </action>
  <verify>
1. File exists: `ls src/quantum_language/qint_comparison.pxi`
2. Contains comparison methods: `grep -c "__eq__\|__lt__\|__gt__\|__le__\|__ge__\|__ne__" src/quantum_language/qint_comparison.pxi` shows 6
3. Line count reasonable: `wc -l src/quantum_language/qint_comparison.pxi` shows 400-550 lines
  </verify>
  <done>qint_comparison.pxi exists with ~470 lines containing all comparison operations</done>
</task>

<task type="auto">
  <name>Task 2: Create qint_division.pxi</name>
  <files>src/quantum_language/qint_division.pxi</files>
  <action>
Extract division operations from qint.pyx (lines ~2035-2433) into a new include file.

The file should contain:
1. A header comment explaining this is an include file for qint class
2. `__floordiv__` method (floor division)
3. `_floordiv_quantum` helper method
4. `__mod__` method (modulo)
5. `_mod_quantum` helper method
6. `__divmod__` method
7. `_divmod_quantum` helper method
8. `__rfloordiv__`, `__rmod__`, `__rdivmod__` reverse methods

Important notes:
- Do NOT include any imports - those stay in qint.pyx
- Do NOT include the cdef class declaration - just the methods
- Methods will be indented at class level (one tab) when included
- Use same formatting/style as original qint.pyx
- Include all docstrings
  </action>
  <verify>
1. File exists: `ls src/quantum_language/qint_division.pxi`
2. Contains division methods: `grep -c "__floordiv__\|__mod__\|__divmod__" src/quantum_language/qint_division.pxi` shows >= 3
3. Line count reasonable: `wc -l src/quantum_language/qint_division.pxi` shows 350-450 lines
  </verify>
  <done>qint_division.pxi exists with ~400 lines containing all division operations</done>
</task>

<task type="auto">
  <name>Task 3: Refactor qint.pyx to use include directives</name>
  <files>src/quantum_language/qint.pyx</files>
  <action>
Refactor qint.pyx to remove the extracted methods and replace them with include directives.

The refactored qint.pyx should contain:
1. All imports (lines 1-41) - KEEP AS-IS
2. cdef class qint(circuit) declaration and class-level attributes
3. `__init__` method (~200 lines)
4. Properties (`width`)
5. UTILITY AND TRACKING METHODS section:
   - `add_dependency`, `get_live_parents`
   - `_do_uncompute`, `uncompute`, `keep`, `_check_not_uncomputed`
   - `print_circuit`
6. `__del__`, `__str__` methods
7. Context manager: `__enter__`, `__exit__`
8. `measure` method

At the end of the class, add include directives:
```cython
	# ====================================================================
	# INCLUDED OPERATION MODULES
	# ====================================================================

	# Arithmetic operations: add, sub, mul
	include "qint_arithmetic.pxi"

	# Bitwise operations: and, or, xor, not, getitem
	include "qint_bitwise.pxi"

	# Comparison operations: eq, ne, lt, gt, le, ge
	include "qint_comparison.pxi"

	# Division operations: floordiv, mod, divmod
	include "qint_division.pxi"
```

Key requirements:
- Remove all code that was extracted to .pxi files
- Add include directives at the END of the class definition (indented at class level)
- Keep all imports at the top (qbool is imported inside methods that need it)
- Preserve all comments and section markers
- Verify indentation is correct (include statements need one tab)
  </action>
  <verify>
1. Line count reduced: `wc -l src/quantum_language/qint.pyx` shows 350-500 lines
2. Include directives present: `grep -c 'include.*qint_' src/quantum_language/qint.pyx` shows 4
3. Build succeeds: `cd /Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum\ Programming\ Language/Quantum_Assembly/python-backend && pip install -e . --no-build-isolation 2>&1 | tail -5`
4. Import works: `python -c "import quantum_language as ql; a = ql.qint(5); b = ql.qint(3); print(a + b)"`
5. Tests pass: `cd /Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum\ Programming\ Language/Quantum_Assembly && python -m pytest tests/python/test_api_coverage.py -v --tb=short 2>&1 | tail -20`
  </verify>
  <done>
qint.pyx is ~400 lines with include directives; all 4 .pxi files included; build passes; tests pass
  </done>
</task>

</tasks>

<verification>
1. All .pxi files exist: qint_arithmetic.pxi, qint_bitwise.pxi, qint_comparison.pxi, qint_division.pxi
2. qint.pyx uses all 4 include directives
3. qint.pyx is under 500 lines
4. No .pxi file exceeds 600 lines
5. Package builds without errors
6. All existing tests pass
7. Basic operations work: `ql.qint(5) + ql.qint(3)`, `ql.qint(5) & 3`, `ql.qint(5) == 5`, `ql.qint(17) // 5`
</verification>

<success_criteria>
- qint.pyx reduced from 2432 lines to ~400 lines
- 4 include files created, each under 600 lines
- No single Cython file exceeds 600 lines (relaxed from 300 for include structure)
- All functionality preserved (build + tests pass)
- Include directive pattern follows Cython best practices
</success_criteria>

<output>
After completion, create `.planning/phases/21-package-restructuring/21-07-SUMMARY.md`
</output>

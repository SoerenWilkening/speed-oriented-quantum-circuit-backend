---
phase: 21-package-restructuring
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_language/qint_arithmetic.pxi
  - src/quantum_language/qint_bitwise.pxi
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "qint arithmetic operations (add, sub, mul) are extracted to separate include file"
    - "qint bitwise operations (and, or, xor, not) are extracted to separate include file"
  artifacts:
    - path: "src/quantum_language/qint_arithmetic.pxi"
      provides: "Arithmetic operations for qint class"
      contains: "__add__|__sub__|__mul__|__iadd__|__isub__|__imul__"
      target_lines: 350-450
    - path: "src/quantum_language/qint_bitwise.pxi"
      provides: "Bitwise operations for qint class"
      contains: "__and__|__or__|__xor__|__invert__|__getitem__"
      target_lines: 450-550
  key_links: []
---

<objective>
Extract arithmetic and bitwise operations from qint.pyx into Cython include files (.pxi).

Purpose: Address gap from verification - qint.pyx is 2432 lines (8x over ~300 line target). Cython include files allow splitting a cdef class across multiple files while maintaining a single compilation unit.

Output: qint_arithmetic.pxi (~400 lines) and qint_bitwise.pxi (~500 lines)

Note: This is a PREPARATORY plan. The .pxi files created here cannot be compiled or verified independently - they will be integrated into qint.pyx in Plan 21-07 via include directives. This plan extracts the code; the next plan wires it together.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-package-restructuring/21-VERIFICATION.md
@src/quantum_language/qint.pyx
@src/quantum_language/qint.pxd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create qint_arithmetic.pxi</name>
  <files>src/quantum_language/qint_arithmetic.pxi</files>
  <action>
Extract arithmetic operations from qint.pyx (lines ~674-1055) into a new include file.

The file should contain:
1. A header comment explaining this is an include file for qint class
2. The `addition_inplace` cdef method
3. `__add__`, `__radd__`, `__iadd__` methods
4. `__sub__`, `__isub__` methods
5. The `multiplication_inplace` cdef method
6. `__mul__`, `__rmul__`, `__imul__` methods

Important notes:
- Do NOT include any imports - those stay in qint.pyx
- Do NOT include the cdef class declaration - just the methods
- Methods will be indented at class level (one tab) when included
- Use same formatting/style as original qint.pyx
- Include all docstrings
  </action>
  <verify>
1. File exists: `ls src/quantum_language/qint_arithmetic.pxi`

2. ALL method signatures present (exact count = 10):
   - `grep -c "cdef addition_inplace" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __add__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __radd__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __iadd__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __sub__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __isub__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "cdef multiplication_inplace" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __mul__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __rmul__" src/quantum_language/qint_arithmetic.pxi` = 1
   - `grep -c "def __imul__" src/quantum_language/qint_arithmetic.pxi` = 1

3. Implementation completeness (key internal calls):
   - `grep -c "self.addition_inplace" src/quantum_language/qint_arithmetic.pxi` >= 2 (called in __iadd__, __isub__)
   - `grep -c "self.multiplication_inplace" src/quantum_language/qint_arithmetic.pxi` >= 2 (called in __mul__, __rmul__)
   - `grep -c "run_instruction" src/quantum_language/qint_arithmetic.pxi` >= 4 (used in actual implementations)

4. Line count reasonable: `wc -l src/quantum_language/qint_arithmetic.pxi` shows 350-450 lines
  </verify>
  <done>qint_arithmetic.pxi exists with ~400 lines containing all 10 arithmetic method signatures and complete implementations (verified by presence of internal calls)</done>
</task>

<task type="auto">
  <name>Task 2: Create qint_bitwise.pxi</name>
  <files>src/quantum_language/qint_bitwise.pxi</files>
  <action>
Extract bitwise operations from qint.pyx (lines ~1056-1541) into a new include file.

The file should contain:
1. A header comment explaining this is an include file for qint class
2. `__and__`, `__iand__`, `__rand__` methods
3. `__or__`, `__ior__`, `__ror__` methods
4. `__xor__`, `__ixor__`, `__rxor__` methods
5. `__invert__` method
6. `__getitem__` method (qubit access)

Important notes:
- Do NOT include any imports - those stay in qint.pyx
- Do NOT include the cdef class declaration - just the methods
- Methods will be indented at class level (one tab) when included
- Use same formatting/style as original qint.pyx
- Include all docstrings
  </action>
  <verify>
1. File exists: `ls src/quantum_language/qint_bitwise.pxi`

2. ALL method signatures present (exact count = 11):
   - `grep -c "def __and__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __iand__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __rand__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __or__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __ior__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __ror__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __xor__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __ixor__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __rxor__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __invert__" src/quantum_language/qint_bitwise.pxi` = 1
   - `grep -c "def __getitem__" src/quantum_language/qint_bitwise.pxi` = 1

3. Implementation completeness (key internal calls):
   - `grep -c "run_instruction" src/quantum_language/qint_bitwise.pxi` >= 8 (used in __and__, __or__, __xor__ implementations, __invert__, __getitem__)

4. Line count reasonable: `wc -l src/quantum_language/qint_bitwise.pxi` shows 450-550 lines
  </verify>
  <done>qint_bitwise.pxi exists with ~500 lines containing all 11 bitwise method signatures and complete implementations (verified by presence of run_instruction calls)</done>
</task>

</tasks>

<verification>
1. Both .pxi files exist in src/quantum_language/
2. Combined line count is ~900 lines (approximately what was extracted)
3. Files contain only method definitions, no imports or class declarations
4. Formatting matches qint.pyx style (tab indentation)
5. All 21 methods (10 arithmetic + 11 bitwise) have signatures present
6. Implementations are complete (verified by internal function calls)

Note: These .pxi files CANNOT be compiled independently. They are Cython include files that will be textually inserted into qint.pyx by Plan 21-07. Verification of correct compilation occurs after Plan 21-07 completes the integration.
</verification>

<success_criteria>
- qint_arithmetic.pxi contains all 10 arithmetic operations (~400 lines)
- qint_bitwise.pxi contains all 11 bitwise operations (~500 lines)
- Files use proper .pxi format for Cython includes
- Extraction verified complete via method signature and internal call checks
- Code is extracted (ready for inclusion by Plan 21-07)
</success_criteria>

<output>
After completion, create `.planning/phases/21-package-restructuring/21-06-SUMMARY.md`
</output>

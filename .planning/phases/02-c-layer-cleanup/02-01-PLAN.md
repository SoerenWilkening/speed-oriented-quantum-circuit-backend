---
phase: 02-c-layer-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/src/Integer.c
  - Backend/src/LogicOperations.c
  - Backend/src/IntegerComparison.c
  - Backend/src/gate.c
autonomous: true

must_haves:
  truths:
    - "All malloc calls use sizeof(type) not sizeof(pointer)"
    - "All sequence_t structures have properly allocated seq and gates_per_layer arrays before access"
    - "Characterization tests still pass after fixes"
  artifacts:
    - path: "Backend/src/Integer.c"
      provides: "Fixed sizeof in INT() and BOOL() functions"
      contains: "sizeof(quantum_int_t)"
    - path: "Backend/src/LogicOperations.c"
      provides: "Fixed sizeof and sequence initialization"
      contains: "sizeof(sequence_t)"
    - path: "Backend/src/gate.c"
      provides: "Fixed sizeof in cx_gate() and ccx_gate()"
      contains: "sizeof(sequence_t)"
  key_links:
    - from: "Backend/src/Integer.c"
      to: "quantum_int_t type"
      via: "malloc sizeof"
      pattern: "malloc.*sizeof.*quantum_int_t"
---

<objective>
Fix critical sizeof() bugs and uninitialized sequence structures in C backend

Purpose: These bugs cause memory corruption and undefined behavior. INT() and BOOL() allocate wrong-sized memory (sizeof pointer instead of sizeof struct), and many sequence functions access uninitialized array pointers.

Output: All sizeof() calls corrected to use actual type sizes, and all sequence_t allocations properly initialize seq and gates_per_layer arrays before access.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

# Key files with bugs
@Backend/src/Integer.c
@Backend/src/LogicOperations.c
@Backend/src/IntegerComparison.c
@Backend/src/gate.c
@Backend/include/QPU.h (for quantum_int_t definition)
@Backend/include/gate.h (for sequence_t definition)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sizeof() bugs in Integer.c and gate.c</name>
  <files>Backend/src/Integer.c, Backend/src/gate.c</files>
  <action>
Fix incorrect sizeof() usage that allocates wrong amount of memory:

**Integer.c:**
- Line 32 in INT(): Change `malloc(sizeof(integer))` to `malloc(sizeof(quantum_int_t))`
- Line 38 in BOOL(): Change `malloc(sizeof(integer))` to `malloc(sizeof(quantum_int_t))`
- Note: `integer` is a local pointer variable, so sizeof(integer) = 8 bytes (pointer size)
- But quantum_int_t contains char MSB + qubit_t q_address[INTEGERSIZE], which is larger

**gate.c:**
- Line 200 in cx_gate(): Change `malloc(sizeof(sequence_t *))` to `malloc(sizeof(sequence_t))`
- Line 210 in ccx_gate(): Change `malloc(sizeof(sequence_t *))` to `malloc(sizeof(sequence_t))`
- Note: These allocate pointer size (8 bytes) instead of full sequence_t struct

After fixing sizeof, also ensure the sequence_t structures in cx_gate() and ccx_gate() have their gates_per_layer and seq arrays properly allocated (they currently access unallocated memory after the fix).
  </action>
  <verify>
Run: `cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly" && make clean && make`

Check that all sizeof calls in these files use type names not variable names:
`grep -n "sizeof(" Backend/src/Integer.c Backend/src/gate.c`
  </verify>
  <done>
- INT() and BOOL() allocate sizeof(quantum_int_t) bytes
- cx_gate() and ccx_gate() allocate sizeof(sequence_t) and properly initialize arrays
- Code compiles without warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix sizeof() and sequence initialization in LogicOperations.c</name>
  <files>Backend/src/LogicOperations.c</files>
  <action>
Fix incorrect sizeof() and uninitialized sequence arrays in LogicOperations.c:

**sizeof bugs to fix:**
- Line 18 in branch_seq(): `malloc(sizeof(sequence_t *))` -> `malloc(sizeof(sequence_t))`
- Line 30 in ctrl_branch_seq(): Same fix
- Line 240 in qq_xor_seq(): Same fix
- Line 256 in cqq_xor_seq(): Same fix

**Uninitialized sequence arrays to fix:**
These functions malloc sequence_t but access seq->seq[x][y] and seq->gates_per_layer[x] without allocating those arrays. For each function, add proper allocation after the sequence_t malloc:

Functions needing seq and gates_per_layer allocation:
- branch_seq() (lines 18-28)
- ctrl_branch_seq() (lines 30-41)
- q_and_seq() (lines 98-114) - accesses seq->seq[0] and seq->gates_per_layer without alloc
- cq_and_seq() (lines 143-159) - accesses seq->seq and gates_per_layer without alloc
- cqq_and_seq() (lines 160-187) - accesses seq->seq and gates_per_layer without alloc
- q_xor_seq() (lines 200-214) - accesses without alloc
- cq_xor_seq() (lines 215-237) - accesses without alloc
- qq_xor_seq() (lines 238-253) - accesses without alloc
- cqq_xor_seq() (lines 254-273) - accesses without alloc
- q_or_seq() (lines 279-308) - accesses without alloc
- cq_or_seq() (lines 344-374) - accesses without alloc
- cqq_or_seq() (lines 375-412) - accesses without alloc

Pattern to apply after malloc(sizeof(sequence_t)):
```c
seq->gates_per_layer = calloc(seq->num_layer, sizeof(num_t));
seq->seq = calloc(seq->num_layer, sizeof(gate_t *));
for (int i = 0; i < seq->num_layer; ++i) {
    seq->seq[i] = calloc(EXPECTED_GATES_PER_LAYER, sizeof(gate_t));
}
```

Use appropriate num_layer and gates count based on what each function needs.
  </action>
  <verify>
Run: `cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly" && make clean && make`

Check no sizeof(sequence_t *) remains:
`grep -n "sizeof(sequence_t \*)" Backend/src/LogicOperations.c` (should return nothing)

Check seq arrays are allocated before use:
`grep -n "->seq\[" Backend/src/LogicOperations.c` and verify each has prior calloc
  </verify>
  <done>
- All sizeof(sequence_t *) changed to sizeof(sequence_t)
- All sequence_t structures have gates_per_layer and seq arrays allocated before access
- Code compiles without warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix sequence initialization in IntegerComparison.c and run tests</name>
  <files>Backend/src/IntegerComparison.c</files>
  <action>
Fix uninitialized sequence arrays in IntegerComparison.c:

**CQ_equal() (lines 11-100):**
- Line 15: malloc(sizeof(sequence_t)) but seq->seq and seq->gates_per_layer not allocated
- Line 26: memset on seq->gates_per_layer but it's not allocated
- Lines 30, 41, 54, 63, 69, 82, 93: Access seq->seq[x][y] without allocation
- Fix: After malloc, allocate gates_per_layer and seq arrays based on num_layer

**cCQ_equal() (lines 101-190):**
- Same pattern of issues
- Line 105: malloc without array allocation
- Line 116: memset on unallocated pointer
- Lines 120, 131, 144, 153, 159, 172, 183: Access unallocated arrays
- Fix: Same pattern as CQ_equal()

After fixing, run characterization tests to ensure behavior is preserved:
```bash
cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly"
make clean && make
pytest tests/python/ -v
```
  </action>
  <verify>
Run:
```bash
cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly"
make clean && make
pytest tests/python/ -v
```

All tests should pass. Check that:
- CQ_equal() and cCQ_equal() properly allocate seq arrays
- No segmentation faults during test execution
  </verify>
  <done>
- CQ_equal() and cCQ_equal() properly allocate seq->gates_per_layer and seq->seq arrays
- All characterization tests pass
- No memory access violations (Valgrind clean if run)
  </done>
</task>

</tasks>

<verification>
1. `make clean && make` succeeds without warnings
2. `grep -rn "sizeof(.*\*)" Backend/src/` shows no sizeof(pointer) patterns for these types
3. `pytest tests/python/ -v` all tests pass
4. Optional: `make valgrind` shows reduced memory errors
</verification>

<success_criteria>
- FOUND-03 satisfied: All sizeof() usage corrected to use sizeof(type) not sizeof(pointer)
- FOUND-04 partially satisfied: All sequence_t structures have arrays allocated before access
- Characterization tests still pass (no behavioral regression)
</success_criteria>

<output>
After completion, create `.planning/phases/02-c-layer-cleanup/02-01-SUMMARY.md`
</output>

---
phase: 76-gate-primitive-exposure
plan: 06
type: execute
wave: 2
depends_on: [76-04, 76-05]
files_modified: []
autonomous: false
requirements: [PRIM-01, PRIM-02, PRIM-03]
gap_closure: true

must_haves:
  truths:
    - "Package rebuilds successfully with all three fixes applied"
    - "pytest reports 10/10 tests passing in test_branch_superposition.py"
    - "test_multiple_indexed_branches: all four states have P~0.25"
    - "test_controlled_branch_cry: p_01 + p_11 > 0.95"
    - "test_double_branch_accumulates: P(1) > 0.95"
  artifacts:
    - path: "src/quantum_language/qint.cpython-*.so"
      provides: "Compiled qint extension with branch() and fixed __getitem__"
    - path: "src/quantum_language/_gates.cpython-*.so"
      provides: "Compiled _gates extension with emit_h, emit_z, emit_ry, emit_mcz"
  key_links:
    - from: "tests/python/test_branch_superposition.py"
      to: "quantum_language.qint"
      via: "pytest import"
      pattern: "import quantum_language"
---

<objective>
Rebuild the quantum_language package and run the full test suite to verify all three UAT gaps are closed.

This plan is the verification gate after the source fixes in plans 04 and 05. It requires a C compiler (gcc or clang) available in the environment. The rebuild compiles all Cython extensions (.pyx files) and the updated C backend (gate.c with the corrected gates_are_inverse() function).

Purpose: Confirm the three fixes (optimizer inverse check, __getitem__ offset, test bitstring convention) each resolve their corresponding failing test. No new code — build and verify only.

Output: All 10 tests in test_branch_superposition.py passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-gate-primitive-exposure/76-04-SUMMARY.md
@.planning/phases/76-gate-primitive-exposure/76-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild package and run test suite</name>
  <files></files>
  <action>
    Run the package rebuild followed by the full test suite. Execute in order:

    ```bash
    pip install -e . --no-build-isolation 2>&1 | tail -20
    ```

    If the build succeeds (exit 0), immediately run:

    ```bash
    pytest tests/python/test_branch_superposition.py -v 2>&1
    ```

    Expected output: 10 tests pass. The three previously failing tests should now pass:
    - `TestBranchIndexed::test_multiple_indexed_branches` — P~0.25 for all four expected states
    - `TestBranchControlled::test_controlled_branch_cry` — p_01 + p_11 > 0.95
    - `TestBranchAccumulation::test_double_branch_accumulates` — P(1) > 0.95

    If the build fails with a C compiler error, report the exact error output. Do NOT attempt to fix compile errors without user direction — the source changes in plans 04/05 must be syntactically valid before this plan can complete.

    If a test still fails after rebuild, report the exact failure message including the assertion error and actual probability values. Do NOT modify source files in this plan — report failures for diagnosis.
  </action>
  <verify>
    pytest exit code is 0. Output shows "10 passed" (or equivalent — all tests in the file pass).
    Specifically verify these three previously-failing tests now pass:
    - PASSED tests/python/test_branch_superposition.py::TestBranchIndexed::test_multiple_indexed_branches
    - PASSED tests/python/test_branch_superposition.py::TestBranchControlled::test_controlled_branch_cry
    - PASSED tests/python/test_branch_superposition.py::TestBranchAccumulation::test_double_branch_accumulates
  </verify>
  <done>All 10 tests in test_branch_superposition.py pass. Phase 76 UAT gaps are fully closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirmation of test results</name>
  <files></files>
  <action>
    Claude has run the rebuild and pytest suite in Task 1 and reported the results.
    Review the pytest output from Task 1. Confirm all three previously-failing tests now show PASSED.

    If you have a build environment locally, optionally re-run to independently verify:
    ```bash
    pip install -e . --no-build-isolation
    pytest tests/python/test_branch_superposition.py -v
    ```
  </action>
  <verify>
    Three previously-failing tests show PASSED in Claude's output (or your own run):
    - TestBranchIndexed::test_multiple_indexed_branches
    - TestBranchControlled::test_controlled_branch_cry
    - TestBranchAccumulation::test_double_branch_accumulates
  </verify>
  <done>User has confirmed all tests pass. Phase 76 gap closure is complete.</done>
  <what-built>
    Package rebuilt with three fixes:
    1. gates_are_inverse() in gate.c now requires negated angles for Ry/Rx/Rz to be inverses
    2. __getitem__ in qint_bitwise.pxi uses right-aligned offset 64-self.bits+item
    3. TestBranchControlled now checks Qiskit little-endian bitstrings "01"/"11"

    All 10 tests in test_branch_superposition.py reported as passing by Claude.
  </what-built>
  <how-to-verify>
    Review Claude's pytest output from Task 1. Confirm 10 passed, 0 failed.
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass, or describe any remaining failures</resume-signal>
</task>

</tasks>

<verification>
Phase 76 gap closure complete when:
- pip install completes with exit code 0
- pytest shows 10/10 tests passing
- No FAILED or ERROR lines in pytest output
- The three UAT-flagged tests all show PASSED
</verification>

<success_criteria>
- All 10 tests in test_branch_superposition.py pass
- test_multiple_indexed_branches: states 000/001/100/101 each ~0.25 probability
- test_controlled_branch_cry: p_01 + p_11 > 0.95 with each ~0.5
- test_double_branch_accumulates: P(1) > 0.95 after two branch(0.5) calls
- Phase 76 UAT status can be updated to "all_passed"
</success_criteria>

<output>
After completion, create `.planning/phases/76-gate-primitive-exposure/76-06-SUMMARY.md`
</output>

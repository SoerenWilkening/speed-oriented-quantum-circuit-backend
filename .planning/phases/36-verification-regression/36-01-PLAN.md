---
phase: 36-verification-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_compare.py
  - tests/test_uncomputation.py
autonomous: true

must_haves:
  truths:
    - "All BUG-CMP-01 xfail markers removed from test_compare.py and test_uncomputation.py"
    - "All BUG-CMP-02 xfail markers removed from test_compare.py and test_uncomputation.py"
    - "No xfail markers for BUG-ARRAY-INIT remain in any test file"
    - "xfail markers for BUG-COND-MUL-01, BUG-DIV-01, BUG-MOD-REDUCE, BUG-CMP-PRES, and dirty ancilla are preserved"
    - "Full pytest suite passes with zero failures and zero unexpected passes"
  artifacts:
    - path: "tests/test_compare.py"
      provides: "Comparison tests without BUG-CMP-01/02 xfail markers"
    - path: "tests/test_uncomputation.py"
      provides: "Uncomputation tests without BUG-CMP-01/02 xfail markers"
  key_links:
    - from: "tests/test_compare.py"
      to: "C backend comparison ops"
      via: "xfail removal proves fixes are permanent"
---

<objective>
Remove all xfail markers for bugs fixed in v1.6 (BUG-CMP-01, BUG-CMP-02, BUG-ARRAY-INIT) and verify the full test suite passes with no regressions.

Purpose: Final verification that all v1.6 fixes are solid and no existing tests have regressed.
Output: Clean test files with xfail markers only for genuinely deferred bugs, plus a passing full test suite.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-verification-regression/36-RESEARCH.md
@tests/test_compare.py
@tests/test_uncomputation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove BUG-CMP-01 and BUG-CMP-02 xfail markers from test files</name>
  <files>tests/test_compare.py, tests/test_uncomputation.py</files>
  <action>
  In `tests/test_compare.py`:

  1. **Remove the xfail marker definitions** (lines ~305-316):
     - Delete `_XFAIL_EQ`, `_XFAIL_NE`, `_XFAIL_ORDER` variable definitions
     - Delete `_XFAIL_ORDER_SAMPLED` variable definition (line ~381-384)

  2. **Remove the failure prediction functions** (lines ~263-299):
     - Delete `_qq_will_fail()` function entirely
     - Delete `_cq_will_fail()` function entirely

  3. **Remove the failure pair tuples** that fed the prediction functions:
     - Delete `_LT_GE_FAIL_PAIRS` set (the large set of (width, a, b) tuples)
     - Delete `_GT_LE_FAIL_PAIRS` set (the large set of (width, a, b) tuples)

  4. **Simplify the marking functions** to pass all cases through without xfail:
     - Replace `_mark_qq_cases(cases)` with a function that returns `cases` unchanged (or inline the removal)
     - Replace `_mark_cq_cases(cases)` with a function that returns `cases` unchanged
     - Replace `_mark_sampled(cases, cq=False)` with a function that returns `cases` unchanged

     The simplest approach: make these functions return their input list unmodified:
     ```python
     def _mark_qq_cases(cases):
         """All BUG-CMP-01/02 bugs fixed in v1.6 — no xfail needed."""
         return cases

     def _mark_cq_cases(cases):
         """All BUG-CMP-01/02 bugs fixed in v1.6 — no xfail needed."""
         return cases

     def _mark_sampled(cases, cq=False):
         """All BUG-CMP-01/02 bugs fixed in v1.6 — no xfail needed."""
         return cases
     ```

  5. **Update the BUG-CMP-01/02 comment block** (line ~302-303) to note bugs are fixed.

  In `tests/test_uncomputation.py`:

  1. **Remove BUG-CMP-01 xfail markers** from `_make_cmp_result_params()`:
     - The eq/ne cases (lines ~387-405) have `marks=pytest.mark.xfail(reason="BUG-CMP-01: ...")`. Remove the xfail marks so these become normal test params.
     - Specifically: remove the `marks=pytest.mark.xfail(reason="BUG-CMP-01: eq/ne return inverted results", strict=False)` from the eq and ne pytest.param calls in `_make_cmp_result_params()`.

  2. **Remove BUG-CMP-02 xfail markers** from `_make_cmp_result_params()`:
     - The ordering comparison cases (lines ~367-375) have conditional `marks.append(pytest.mark.xfail(reason="BUG-CMP-02: ..."))`. Remove this conditional xfail logic.
     - The `_is_msb_spanning` check and BUG-CMP-02 xfail can be removed from ordering cases.

  3. **Remove BUG-CMP-02 xfail markers** from `_make_cmp_ancilla_params()`:
     - Lines ~468-474 add `BUG-CMP-02` xfail for MSB-spanning values. Remove only the BUG-CMP-02 xfail.
     - **KEEP** the dirty ancilla xfail (`"Comparison {op_name} uses widened temporaries leaving ancilla dirty"`) — this is NOT a bug fix, it's a known limitation.

  4. **Remove the standalone BUG-CMP-01 xfail** from `test_uncomp_compound_eq()`:
     - Line ~581: `@pytest.mark.xfail(reason="BUG-CMP-01: eq returns inverted results", strict=False)` — remove this decorator entirely.

  **CRITICAL — markers to PRESERVE (do NOT touch):**
  - `tests/test_conditionals.py`: BUG-COND-MUL-01 markers (deferred bug)
  - `tests/test_bitwise_mixed.py`: `_CQ_MIXED_WIDTH_XFAIL` (unrelated limitation)
  - `tests/test_mod.py`: BUG-MOD-REDUCE markers (deferred bug)
  - `tests/test_div.py`: BUG-DIV-01 markers (deferred bug)
  - `tests/test_modular.py`: Related deferred bug markers
  - `tests/test_compare_preservation.py`: BUG-CMP-PRES-01/02 markers (calibration issues, separate from CMP-01/02)
  - `tests/test_uncomputation.py`: Dirty ancilla xfail on gt/le (known limitation, not a bug)
  </action>
  <verify>
  Run: `grep -c "BUG-CMP-01\|BUG-CMP-02" tests/test_compare.py tests/test_uncomputation.py`
  Expected: 0 matches in both files (all BUG-CMP-01/02 references removed).

  Run: `grep -c "BUG-COND-MUL\|BUG-DIV\|BUG-MOD\|BUG-CMP-PRES\|ancilla dirty\|widened temporaries" tests/test_conditionals.py tests/test_uncomputation.py tests/test_mod.py tests/test_div.py tests/test_compare_preservation.py`
  Expected: Non-zero counts confirming deferred bug markers are preserved.
  </verify>
  <done>All BUG-CMP-01 and BUG-CMP-02 xfail markers removed from test_compare.py and test_uncomputation.py. No deferred bug markers accidentally removed.</done>
</task>

<task type="auto">
  <name>Task 2: Run full regression suite and verify zero failures</name>
  <files>(no files modified — verification only)</files>
  <action>
  Run the complete test suite and verify results:

  1. Run: `pytest tests/ -v --tb=short 2>&1 | tail -50`
     - Look at the summary line for failures, errors, xfail, xpass counts.

  2. Expected outcome:
     - **0 failures** and **0 errors**
     - **0 unexpected passes (xpass)** — if any xpass appear, it means an xfail marker was left that should have been removed
     - Remaining xfail count should be LOWER than before (the BUG-CMP-01/02 xfails are gone)
     - All previously passing tests still pass (no regressions)

  3. If any failures occur:
     - Check if they're in comparison tests (would indicate incomplete marker removal or a regression)
     - Check if they're in unrelated tests (would indicate a regression from Phase 34/35 changes)
     - Fix any issues found

  4. Run: `pytest tests/ -v --tb=short 2>&1 | grep -c "XPASS"`
     - Expected: 0 (no unexpected passes — all fixed tests should now be normal passes, not xpass)
  </action>
  <verify>
  pytest summary line shows: 0 failed, 0 errors, 0 xpass.
  Total test count approximately matches previous run count (no tests accidentally deleted).
  </verify>
  <done>Full test suite passes with zero failures, zero errors, and zero unexpected passes. All v1.6 fixes verified as permanent.</done>
</task>

</tasks>

<verification>
1. `grep -rn "BUG-CMP-01\|BUG-CMP-02" tests/test_compare.py tests/test_uncomputation.py` returns no matches
2. `grep -rn "BUG-COND-MUL\|BUG-DIV\|BUG-MOD" tests/` returns matches (deferred bugs preserved)
3. `pytest tests/ -v` shows 0 failures, 0 errors, 0 xpass
4. `grep -rn "BUG-ARRAY-INIT" tests/` returns only historical comments, no xfail markers
</verification>

<success_criteria>
- All BUG-CMP-01 and BUG-CMP-02 xfail markers removed from test_compare.py and test_uncomputation.py
- No BUG-ARRAY-INIT xfail markers remain in any test file
- Deferred bug markers (BUG-COND-MUL-01, BUG-DIV-01, BUG-MOD-REDUCE, BUG-CMP-PRES) preserved
- Full pytest suite: 0 failures, 0 errors, 0 unexpected passes
- No previously passing tests have regressed
</success_criteria>

<output>
After completion, create `.planning/phases/36-verification-regression/36-01-SUMMARY.md`
</output>

---
phase: 09-code-organization
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - Backend/include/module_deps.md
  - Backend/include/circuit.h
  - python-backend/quantum_language.pxd
autonomous: true

must_haves:
  truths:
    - "Module dependencies documented for new operation headers"
    - "circuit.h includes the new operation headers for convenience"
    - "Cython .pxd file reflects new header organization"
    - "Full test suite passes (all phases)"
  artifacts:
    - path: "Backend/include/module_deps.md"
      provides: "Updated module dependency documentation"
      contains: "arithmetic_ops|comparison_ops|bitwise_ops"
  key_links:
    - from: "Backend/include/circuit.h"
      to: "arithmetic_ops.h, comparison_ops.h, bitwise_ops.h"
      via: "#include or documented exclusion"
      pattern: "operations"
    - from: "python-backend/quantum_language.pxd"
      to: "arithmetic_ops.h, bitwise_ops.h"
      via: 'cdef extern from'
      pattern: 'extern from "(arithmetic|bitwise|comparison)_ops.h"'
---

<objective>
Update module documentation and verify full integration of the new category-based operation headers.

Purpose: Complete CODE-04 by documenting the new module structure, optionally updating circuit.h, and ensuring Cython bindings reflect the organization.

Output: Updated module_deps.md, verified integration, all tests passing.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-code-organization/09-01-SUMMARY.md
@.planning/phases/09-code-organization/09-02-SUMMARY.md
@.planning/phases/09-code-organization/09-03-SUMMARY.md
@Backend/include/module_deps.md
@Backend/include/circuit.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update module_deps.md with new operation headers</name>
  <files>Backend/include/module_deps.md</files>
  <action>
Update the module dependency documentation to include the new category-based operation headers:

1. Update the Module Overview table to add:
   | Module | Purpose | Lines (Header/Source) |
   | arithmetic_ops.h | Addition, subtraction, multiplication operations | ~50 / - |
   | comparison_ops.h | Equality and ordering comparisons | ~50 / - |
   | bitwise_ops.h | Width-parameterized bitwise operations | ~50 / - |

2. Update the dependency graph ASCII art:
   ```
                       types.h
                          |
              +-----------+-----------+
              |           |           |
           gate.h  qubit_allocator.h  |
              |           |           |
              +-----------+-----------+
                          |
     +--------------------+--------------------+
     |                    |                    |
   arithmetic_ops.h  comparison_ops.h  bitwise_ops.h
     |                    |                    |
     +--------------------+--------------------+
                          |
                     optimizer.h
                          |
                    circuit_output.h
                          |
                      circuit.h  <-- Main API
   ```

3. Add a new section "Operation Modules" describing:
   - arithmetic_ops.h: QQ_add, CQ_add, QQ_mul, CQ_mul and variants
   - comparison_ops.h: QQ_equal, QQ_less_than, CQ_equal_width, CQ_less_than
   - bitwise_ops.h: Q_not, Q_xor, Q_and, Q_or and controlled variants

4. Update Legacy Headers section to include:
   - Integer.h: Now includes arithmetic_ops.h (kept for backward compat)
   - IntegerComparison.h: Now includes comparison_ops.h (backward compat wrapper)
   - LogicOperations.h: Now includes bitwise_ops.h (backward compat wrapper)

5. Update Module Responsibilities section with entries for the three new headers

6. Add Phase 9 context to Historical Context section
  </action>
  <verify>grep -c "arithmetic_ops\|comparison_ops\|bitwise_ops" Backend/include/module_deps.md returns 6+ matches</verify>
  <done>module_deps.md documents all three new operation headers with dependencies and responsibilities</done>
</task>

<task type="auto">
  <name>Task 2: Update Cython .pxd to use new headers (optional but cleaner)</name>
  <files>python-backend/quantum_language.pxd</files>
  <action>
Update quantum_language.pxd to reflect the new header organization:

1. Add `cdef extern from "arithmetic_ops.h":` block for arithmetic functions
   - Move QQ_add, CQ_add, QQ_mul, CQ_mul declarations here
   - Keep the existing function signatures unchanged

2. Add `cdef extern from "bitwise_ops.h":` block for bitwise functions
   - Move Q_not, cQ_not, Q_xor, cQ_xor, Q_and, CQ_and, Q_or, CQ_or here
   - Keep existing signatures unchanged

3. Keep the existing `cdef extern from "Integer.h":` block but note it now pulls in arithmetic_ops.h
   - Can keep declarations under Integer.h since it includes arithmetic_ops.h (both work)
   - OR move to arithmetic_ops.h for cleaner organization

4. Keep the existing `cdef extern from "LogicOperations.h":` block for legacy qbool functions
   - The width-parameterized ops can stay here (LogicOperations.h includes bitwise_ops.h)
   - OR move to bitwise_ops.h block for cleaner organization

IMPORTANT: Either approach works because the backward compat wrappers include the new headers.
The cleaner approach is to reference the new headers directly, but it's not required.

Choose the cleaner approach: Update extern blocks to reference the new header files.
  </action>
  <verify>Cython extension builds: cd python-backend && python setup.py build_ext --inplace</verify>
  <done>quantum_language.pxd updated to reference new operation headers (or verified working with existing structure)</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify CODE-04 compliance</name>
  <files>tests/</files>
  <action>
1. Rebuild Cython extension with clean build:
   ```bash
   cd python-backend && rm -f *.so && python setup.py build_ext --inplace
   ```

2. Run full test suite covering all phases:
   ```bash
   cd tests && python -m pytest test_phase*.py -v --tb=short 2>&1 | head -100
   ```

3. Check test counts match expectations:
   - Phase 1-4: Foundation tests (~59)
   - Phase 5: Variable-width tests (~20)
   - Phase 6: Bitwise tests (~88)
   - Phase 7: Arithmetic/comparison tests (~38)
   - Phase 8: Stats/optimization tests (~18)
   Total: ~200+ tests should pass

4. Verify CODE-04 compliance:
   - Arithmetic operations organized: arithmetic_ops.h (check)
   - Comparison operations organized: comparison_ops.h (check)
   - Logic operations organized: bitwise_ops.h (check)
   - Module structure consistent: All follow types.h -> ops.h pattern (check)

5. Update REQUIREMENTS.md to mark CODE-04 as complete:
   - [ ] CODE-04 -> [x] CODE-04
  </action>
  <verify>Full test suite passes; CODE-04 requirement marked complete</verify>
  <done>All tests pass; CODE-04 (operations organized by category) verified complete</done>
</task>

</tasks>

<verification>
1. `grep -c "arithmetic_ops\|comparison_ops\|bitwise_ops" Backend/include/module_deps.md` shows documentation
2. Cython extension builds successfully
3. Full test suite passes (200+ tests)
4. `grep "CODE-04" .planning/REQUIREMENTS.md` shows `[x]` (complete)
5. New headers follow consistent pattern (types.h dependency, ~50 lines each)
</verification>

<success_criteria>
- module_deps.md updated with new operation headers in dependency graph
- Cython extension builds with new header organization
- Full test suite passes (all phases)
- CODE-04 requirement marked complete in REQUIREMENTS.md
- Phase 9 success criteria met: operations organized by category
</success_criteria>

<output>
After completion, create `.planning/phases/09-code-organization/09-04-SUMMARY.md`
</output>

---
status: complete
phase: 107-call-graph-dag-foundation
source: [107-01-SUMMARY.md, 107-02-SUMMARY.md]
started: 2026-03-06T00:00:00Z
updated: 2026-03-06T00:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Import CallGraphDAG from package
expected: `from quantum_language import CallGraphDAG` imports without error. CallGraphDAG is available as a public API class.
result: pass

### 2. Compile with opt=1 builds DAG
expected: Decorate a function with `@ql.compile(opt=1)`, call it, then access `.call_graph` on the compiled function. It should return a CallGraphDAG instance (not None).
result: pass

### 3. DAG nodes contain qubit and gate data
expected: After calling a compiled function (opt=1), inspect `func.call_graph`. The DAG should contain nodes with correct qubit_set and gate_count reflecting the operations performed.
result: pass

### 4. Nested calls create hierarchical DAG edges
expected: If compiled function A calls compiled function B (both opt=1), the resulting DAG should have a call edge from A's node to B's node, showing the call hierarchy.
result: pass

### 5. Parallel group detection via overlap
expected: `call_graph.parallel_groups()` returns groups of nodes whose qubit sets do NOT overlap, meaning they can execute in parallel. Non-overlapping sibling calls should appear in separate groups.
result: pass

### 6. opt=3 skips DAG building (legacy mode)
expected: Decorate with `@ql.compile(opt=3)` and call it. The `.call_graph` property should be None, confirming DAG overhead is skipped.
result: pass

### 7. Backward compatibility - default behavior unchanged
expected: Existing `@ql.compile()` usage (no opt parameter) still works correctly. Circuits compile and execute as before.
result: pass

### 8. All 43 tests pass
expected: Running `pytest tests/python/test_call_graph.py -v` passes all 43 tests (32 unit + 11 integration).
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]

---
phase: 48-core-capture-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_language/_core.pyx
  - src/quantum_language/_core.pxd
autonomous: true

must_haves:
  truths:
    - "extract_gate_range(start, end) returns a list of gate dicts from the circuit's layer range"
    - "inject_remapped_gates(gates, qubit_map) adds gates to the circuit with remapped qubit indices"
    - "Layer floor is saved/restored around injection to prevent gate reordering"
    - "A cache-clear hook exists that can be called on circuit reset"
    - "Helper functions expose layer_floor get/set and qubit allocation for replay"
  artifacts:
    - path: "src/quantum_language/_core.pyx"
      provides: "extract_gate_range, inject_remapped_gates, layer_floor helpers, allocate_qubit wrapper, cache-clear hook"
      contains: "def extract_gate_range"
    - path: "src/quantum_language/_core.pxd"
      provides: "C-level gate_t field access declarations for Cython"
      contains: "gate_t"
  key_links:
    - from: "extract_gate_range"
      to: "circuit_t.sequence"
      via: "Layer iteration pattern from draw_data()"
      pattern: "circ.sequence\\[layer\\]"
    - from: "inject_remapped_gates"
      to: "add_gate()"
      via: "Gate-by-gate injection with remapped indices"
      pattern: "add_gate"
---

<objective>
Add Cython-level infrastructure for gate capture and replay to `_core.pyx`.

Purpose: Provide the low-level primitives that the Python `@ql.compile` decorator needs -- gate extraction from a layer range, gate injection with qubit remapping, layer floor management, qubit allocation wrapper, and a cache-clear hook callable on circuit reset.

Output: Modified `_core.pyx` and `_core.pxd` with 5-6 new functions ready for import by `compile.py`.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-core-capture-replay/48-RESEARCH.md
@.planning/phases/48-core-capture-replay/48-CONTEXT.md
@src/quantum_language/_core.pyx
@src/quantum_language/_core.pxd
@c_backend/include/types.h
@c_backend/include/circuit.h
@c_backend/include/optimizer.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gate_t field access to _core.pxd and implement extract_gate_range in _core.pyx</name>
  <files>src/quantum_language/_core.pxd, src/quantum_language/_core.pyx</files>
  <action>
  1. In `_core.pxd`, ensure the `gate_t` struct fields are declared so Cython can access them: `Gate` (Standardgate_t), `Target` (qubit_t), `GateValue` (double), `NumControls` (int), `Control[2]` (qubit_t array), `large_control` (qubit_t pointer). Also ensure `circuit_t.sequence`, `circuit_t.used_gates_per_layer`, `circuit_t.used_layer`, `circuit_t.layer_floor` are accessible. Check what's already declared and only add what's missing.

  2. In `_core.pyx`, add a new function `extract_gate_range(int start_layer, int end_layer)` that:
     - Gets the circuit pointer via the existing `_get_circuit()` pattern
     - Iterates layers from `start_layer` to `end_layer` (exclusive)
     - For each gate in each layer, builds a Python dict with keys: `'type'` (int), `'target'` (int), `'angle'` (float), `'num_controls'` (int), `'controls'` (list of int)
     - Handles `large_control` pointer for gates with >2 controls (same pattern as `draw_data()` / `circuit_to_draw_data()` in circuit_output.c)
     - Returns a list of gate dicts
     - Follow the exact extraction pattern from research Pattern 1

  3. Add a `get_current_layer()` function if not already present, returning `circuit_t.used_layer`. Check existing code first -- it may already exist under a different name (e.g., as part of draw_data or circuit stats).
  </action>
  <verify>
  Build with `pip install -e .` from the project root. Import `from quantum_language._core import extract_gate_range, get_current_layer` in Python. Create a simple circuit with a few gates, call `extract_gate_range(0, get_current_layer())`, verify it returns a non-empty list of dicts with correct keys.
  </verify>
  <done>
  `extract_gate_range()` returns correct gate data for a simple circuit. `get_current_layer()` returns the circuit's `used_layer`. Both are importable from `_core`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement inject_remapped_gates, layer_floor helpers, allocate_qubit, and cache-clear hook</name>
  <files>src/quantum_language/_core.pyx</files>
  <action>
  1. Add `inject_remapped_gates(list gates, dict qubit_map)` to `_core.pyx`:
     - For each gate dict in `gates`, allocate a new `gate_t` via `malloc`, populate fields from the dict with qubit indices remapped through `qubit_map`
     - Handle `large_control` allocation for gates with >2 controls
     - Call `add_gate(circ, g)` for each gate (same pattern as `run_instruction()` in execution.c)
     - Follow research Pattern 2

  2. Add layer_floor management helpers:
     - `_get_layer_floor()` -> int: returns `circuit_t.layer_floor`
     - `_set_layer_floor(int floor)`: sets `circuit_t.layer_floor`
     These are simple one-liners wrapping the C field access.

  3. Add `_allocate_qubit()` -> int: wrapper around `allocator_alloc()` that allocates a single qubit and returns its index. Look at how qint.__init__ or the existing allocator is used in `_core.pyx` and follow the same pattern.

  4. Add a module-level function `_clear_compile_caches()` that will be called by external code (compile.py) to register a hook. The simplest approach: maintain a module-level list `_compile_cache_clear_hooks = []` and a function `_register_cache_clear_hook(callback)` and `_clear_compile_caches()` that calls all registered callbacks. In the `circuit` class `__init__` method, after the existing state resets, call `_clear_compile_caches()`.

  5. Rebuild with `pip install -e .` and verify all new functions are importable.
  </action>
  <verify>
  Build with `pip install -e .`. In Python:
  - Import all new functions from `quantum_language._core`
  - Create a circuit, add some gates, call `extract_gate_range()` to get gates
  - Create a trivial qubit_map `{i: i for i in range(N)}` (identity mapping)
  - Call `inject_remapped_gates(gates, qubit_map)` -- verify circuit now has double the gates
  - Verify `_get_layer_floor()` and `_set_layer_floor()` work
  - Verify `_allocate_qubit()` returns an integer
  - Register a mock callback with `_register_cache_clear_hook()`, create a new circuit, verify callback was called
  - Run existing test suite `pytest tests/ -x --timeout=60` to ensure no regressions
  </verify>
  <done>
  All 6 new Cython functions (`extract_gate_range`, `inject_remapped_gates`, `_get_layer_floor`, `_set_layer_floor`, `_allocate_qubit`, `_register_cache_clear_hook` + `_clear_compile_caches`) are importable and functional. Cache-clear hook fires on `ql.circuit()`. Existing tests still pass.
  </done>
</task>

</tasks>

<verification>
1. `pip install -e .` builds successfully with no Cython compilation errors
2. All new functions importable: `from quantum_language._core import extract_gate_range, inject_remapped_gates, get_current_layer, _get_layer_floor, _set_layer_floor, _allocate_qubit, _register_cache_clear_hook`
3. Round-trip test: create circuit -> extract gates -> inject with identity map -> verify doubled gate count
4. Existing test suite passes: `pytest tests/ -x --timeout=60`
</verification>

<success_criteria>
- Cython compiles without errors
- Gate extraction returns correct data for circuits with single-control, multi-control, and rotation gates
- Gate injection with remapping places gates at correct qubit indices
- Layer floor helpers prevent gate reordering during injection
- Cache-clear hook fires on `ql.circuit()` creation
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/48-core-capture-replay/48-01-SUMMARY.md`
</output>

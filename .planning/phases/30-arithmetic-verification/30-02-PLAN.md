---
phase: 30-arithmetic-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_mul.py
autonomous: true

must_haves:
  truths:
    - "QQ multiplication (qint * qint) returns correct product mod 2^width for all input pairs at 1-3 bit widths"
    - "CQ multiplication (qint *= int) returns correct product mod 2^width for all input pairs at 1-3 bit widths"
    - "QQ and CQ multiplication produce correct results for representative pairs at 4-5 bit widths"
  artifacts:
    - path: "tests/test_mul.py"
      provides: "Exhaustive + sampled multiplication verification tests (QQ and CQ variants)"
      contains: "test_qq_mul_exhaustive"
  key_links:
    - from: "tests/test_mul.py"
      to: "tests/conftest.py"
      via: "verify_circuit fixture"
      pattern: "verify_circuit"
---

<objective>
Create exhaustive multiplication verification tests covering QQ and CQ variants at widths 1-3 (exhaustive) and 4-5 (sampled).

Purpose: Satisfy VARITH-03 (multiplication) -- confirm that all multiplication operations produce correct results through the full pipeline.
Output: tests/test_mul.py with all parametrized test cases passing.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-arithmetic-verification/30-CONTEXT.md
@.planning/phases/30-arithmetic-verification/30-RESEARCH.md
@tests/conftest.py
@tests/verify_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exhaustive and sampled multiplication tests</name>
  <files>tests/test_mul.py</files>
  <action>
Create tests/test_mul.py with the following structure:

1. Add `warnings.filterwarnings("ignore", message="Value .* exceeds")` at module level.

2. Generate test case data at MODULE LEVEL:
   - `EXHAUSTIVE_QQ_MUL`: List of (width, a, b) for widths 1-3, using `generate_exhaustive_pairs(width)`.
     Total: 4 + 16 + 64 = 84 pairs.
   - `SAMPLED_QQ_MUL`: List of (width, a, b) for widths 4-5, using `generate_sampled_pairs(width, sample_size=10)` plus boundary pairs.
   - Same data for CQ variants.

3. Create 4 parametrized test functions:
   - `test_qq_mul_exhaustive(verify_circuit, width, a, b)`: QQ multiplication at 1-3 bits
     - Build circuit: `qa = ql.qint(a, width=w); qb = ql.qint(b, width=w); _r = qa * qb`
     - Expected: `(a * b) % (1 << w)`
     - Use default argument binding: `def circuit_builder(a=a, b=b, w=width):`
   - `test_qq_mul_sampled(verify_circuit, width, a, b)`: QQ multiplication at 4-5 bits
   - `test_cq_mul_exhaustive(verify_circuit, width, a, b)`: CQ multiplication at 1-3 bits
     - Build circuit: `qa = ql.qint(a, width=w); qa *= b`
     - Expected: `(a * b) % (1 << w)`
   - `test_cq_mul_sampled(verify_circuit, width, a, b)`: CQ multiplication at 4-5 bits

4. Use `format_failure_message()` in all assertions.

Important: Multiplication circuits are larger than addition. Widths 1-3 are exhaustive (84 pairs/variant). Widths 4-5 are sampled (~30-40 pairs/variant) to keep runtime reasonable.
  </action>
  <verify>Run `python -m pytest tests/test_mul.py -v --tb=short 2>&1 | tail -20` and confirm all tests pass. Expected: ~84 exhaustive + ~30-40 sampled per variant = ~250 total tests.</verify>
  <done>All QQ and CQ multiplication tests pass for exhaustive widths 1-3 and sampled widths 4-5.</done>
</task>

</tasks>

<verification>
Run:
```bash
python -m pytest tests/test_mul.py -v --tb=short 2>&1 | tail -20
```
All tests must pass.
</verification>

<success_criteria>
- Multiplication verified for all input pairs at 1-3 bit widths (QQ and CQ variants)
- Multiplication verified for representative pairs at 4-5 bit widths
- All tests use the verify_circuit fixture from Phase 28 framework
- Products correctly wrap unsigned: `(a * b) % (1 << width)`
</success_criteria>

<output>
After completion, create `.planning/phases/30-arithmetic-verification/30-02-SUMMARY.md`
</output>

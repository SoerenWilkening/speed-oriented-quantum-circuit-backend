---
phase: 06-bit-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/src/LogicOperations.c
  - Backend/include/LogicOperations.h
autonomous: true

must_haves:
  truths:
    - "Q_not(bits) applies X gates to all bits of a quantum integer"
    - "Q_xor(bits) applies CNOT gates for in-place XOR of two quantum integers"
    - "cQ_not(bits) applies controlled-X gates with single control qubit"
    - "cQ_xor(bits) applies controlled-CNOT (Toffoli) gates with single control qubit"
  artifacts:
    - path: "Backend/src/LogicOperations.c"
      provides: "Width-parameterized NOT and XOR sequence generators"
      contains: "Q_not, Q_xor, cQ_not, cQ_xor"
    - path: "Backend/include/LogicOperations.h"
      provides: "Function declarations for width-parameterized operations"
      contains: "Q_not, Q_xor, cQ_not, cQ_xor"
  key_links:
    - from: "Q_not"
      to: "gate.h x()"
      via: "X gate application per bit"
      pattern: "x\\(&seq->seq"
    - from: "Q_xor"
      to: "gate.h cx()"
      via: "CNOT gate application per bit"
      pattern: "cx\\(&seq->seq"
---

<objective>
Implement width-parameterized NOT and XOR operations in the C layer.

Purpose: NOT (~) and XOR (^) are the simpler bitwise operations as they don't require ancilla allocation. NOT applies X gates to all bits in-place. XOR applies CNOT gates where target is modified by source.

Output: Four new sequence generators: Q_not(bits), cQ_not(bits), Q_xor(bits), cQ_xor(bits) following Phase 5 patterns.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bit-operations/06-CONTEXT.md
@.planning/phases/06-bit-operations/06-RESEARCH.md
@Backend/src/LogicOperations.c
@Backend/src/IntegerAddition.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Q_not and cQ_not functions</name>
  <files>Backend/src/LogicOperations.c, Backend/include/LogicOperations.h</files>
  <action>
    Add width-parameterized NOT functions to LogicOperations.c:

    1. Create `Q_not(int bits)` function:
       - Bounds check: if bits < 1 || bits > 64, return NULL
       - Allocate sequence with 1 layer (all X gates in parallel)
       - Allocate gates_per_layer[0] = bits
       - Loop over bits: x(&seq->seq[0][i], i) for each bit
       - Follow standard NULL check/cleanup pattern from CQ_add

    2. Create `cQ_not(int bits)` function:
       - Bounds check: if bits < 1 || bits > 64, return NULL
       - Allocate sequence with bits layers (Toffoli gates sequential)
       - Qubit layout: [0:bits] = target, [bits] = control qubit
       - Loop over bits: cx(&seq->seq[i][0], i, bits) - controlled X gate
       - Follow standard NULL check/cleanup pattern

    3. Add function declarations to LogicOperations.h:
       - sequence_t *Q_not(int bits);
       - sequence_t *cQ_not(int bits);

    Naming convention: Uppercase prefix Q_ for new width-parameterized functions,
    lowercase q_ for legacy INTEGERSIZE functions (keep for backward compat).
  </action>
  <verify>
    - gcc compilation succeeds: cd Backend && make
    - Function signatures appear in header: grep "Q_not\|cQ_not" Backend/include/LogicOperations.h
  </verify>
  <done>Q_not(bits) and cQ_not(bits) compiled without errors, functions create correct gate sequences</done>
</task>

<task type="auto">
  <name>Task 2: Implement Q_xor and cQ_xor functions</name>
  <files>Backend/src/LogicOperations.c, Backend/include/LogicOperations.h</files>
  <action>
    Add width-parameterized XOR functions to LogicOperations.c:

    1. Create `Q_xor(int bits)` function (in-place, target ^= source):
       - Bounds check: if bits < 1 || bits > 64, return NULL
       - Allocate sequence with 1 layer (all CNOTs in parallel - O(1) depth!)
       - Qubit layout: [0:bits] = target, [bits:2*bits] = source
       - Loop over bits: cx(&seq->seq[0][i], i, bits + i) - CNOT with target=i, control=bits+i
       - Follow standard NULL check/cleanup pattern

    2. Create `cQ_xor(int bits)` function (controlled XOR):
       - Bounds check: if bits < 1 || bits > 64, return NULL
       - Allocate sequence with bits layers (Toffoli gates sequential)
       - Qubit layout: [0:bits] = target, [bits:2*bits] = source, [2*bits] = control
       - Loop over bits: ccx(&seq->seq[i][0], i, bits + i, 2*bits) - Toffoli gate
       - Follow standard NULL check/cleanup pattern

    3. Add function declarations to LogicOperations.h:
       - sequence_t *Q_xor(int bits);
       - sequence_t *cQ_xor(int bits);
  </action>
  <verify>
    - gcc compilation succeeds: cd Backend && make
    - Function signatures appear in header: grep "Q_xor\|cQ_xor" Backend/include/LogicOperations.h
  </verify>
  <done>Q_xor(bits) and cQ_xor(bits) compiled without errors, XOR uses CNOT gates, controlled XOR uses Toffoli</done>
</task>

</tasks>

<verification>
All verification must pass before plan completion:

1. Compilation: `cd Backend && make` succeeds without errors
2. Header check: All four new functions declared in LogicOperations.h
3. Pattern verification: New functions follow Phase 5 memory allocation patterns (NULL checks, cleanup on error)
4. Gate check: Q_not uses x(), Q_xor uses cx(), cQ_not uses cx(), cQ_xor uses ccx()
</verification>

<success_criteria>
- Q_not(bits) generates bits parallel X gates
- cQ_not(bits) generates bits sequential controlled-X gates
- Q_xor(bits) generates bits parallel CNOT gates (O(1) depth)
- cQ_xor(bits) generates bits sequential Toffoli gates
- All functions handle width 1-64 and return NULL for invalid widths
- Compilation succeeds with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-bit-operations/06-01-SUMMARY.md`
</output>

---
phase: 74-mcx-ccx-gate-decomposition-sequence-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - c_backend/src/ToffoliAddition.c
  - c_backend/src/ToffoliAdditionCDKM.c
  - c_backend/src/ToffoliAdditionCLA.c
  - c_backend/src/ToffoliAdditionHelpers.c
  - c_backend/include/toffoli_addition_internal.h
  - c_backend/src/hot_path_add.c
  - c_backend/src/hot_path_add_toffoli.c
  - setup.py
autonomous: true
requirements: [INF-03]

must_haves:
  truths:
    - "All existing Toffoli arithmetic tests pass with zero regressions after file split"
    - "ToffoliAddition.c no longer exists -- replaced by CDKM, CLA, and Helpers files"
    - "hot_path_add.c is slimmed down with Toffoli CLA logic extracted"
  artifacts:
    - path: "c_backend/src/ToffoliAdditionCDKM.c"
      provides: "CDKM ripple-carry adder functions (QQ/CQ/cQQ/cCQ, MAJ/UMA helpers, caches)"
      min_lines: 500
    - path: "c_backend/src/ToffoliAdditionCLA.c"
      provides: "BK/KS CLA adder functions (all variants, merge tree, controlled CLA)"
      min_lines: 600
    - path: "c_backend/src/ToffoliAdditionHelpers.c"
      provides: "Shared utilities (alloc_sequence, copy_hardcoded_sequence, toffoli_sequence_free)"
      min_lines: 50
    - path: "c_backend/include/toffoli_addition_internal.h"
      provides: "Shared type defs (bk_merge_t) and internal function declarations"
      contains: "bk_merge_t"
    - path: "c_backend/src/hot_path_add_toffoli.c"
      provides: "Toffoli-specific CLA/RCA selection and dispatch logic"
      min_lines: 100
  key_links:
    - from: "c_backend/src/ToffoliAdditionCDKM.c"
      to: "c_backend/include/toffoli_addition_internal.h"
      via: "#include"
      pattern: '#include "toffoli_addition_internal.h"'
    - from: "c_backend/src/ToffoliAdditionCLA.c"
      to: "c_backend/include/toffoli_addition_internal.h"
      via: "#include"
      pattern: '#include "toffoli_addition_internal.h"'
    - from: "setup.py"
      to: "c_backend/src/ToffoliAdditionCDKM.c"
      via: "source file list"
      pattern: "ToffoliAdditionCDKM"
---

<objective>
Pure behavior-preserving refactoring of ToffoliAddition.c (1968 lines) into three focused modules and extraction of Toffoli CLA dispatch logic from hot_path_add.c.

Purpose: Make the codebase maintainable before adding MCX decomposition and CCX->Clifford+T features. User explicitly requested refactoring FIRST as a separate plan.
Output: Three new C files replacing ToffoliAddition.c, one extracted file from hot_path_add.c, one internal header, updated build config.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-mcx-ccx-gate-decomposition-sequence-refactoring/74-CONTEXT.md
@.planning/phases/74-mcx-ccx-gate-decomposition-sequence-refactoring/74-RESEARCH.md
@c_backend/src/ToffoliAddition.c
@c_backend/src/hot_path_add.c
@c_backend/include/toffoli_arithmetic_ops.h
@c_backend/include/toffoli_sequences.h
@setup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split ToffoliAddition.c into CDKM, CLA, and Helpers modules</name>
  <files>
    c_backend/src/ToffoliAddition.c
    c_backend/src/ToffoliAdditionCDKM.c
    c_backend/src/ToffoliAdditionCLA.c
    c_backend/src/ToffoliAdditionHelpers.c
    c_backend/include/toffoli_addition_internal.h
  </files>
  <action>
Split ToffoliAddition.c (1968 lines) into three focused files. This is a PURE REFACTOR -- no logic changes, no new features, no behavior modifications.

**Create `c_backend/include/toffoli_addition_internal.h`:**
- Header guard: `TOFFOLI_ADDITION_INTERNAL_H`
- Move `bk_merge_t` struct definition here (currently local to ToffoliAddition.c)
- Declare shared internal functions: `alloc_sequence()`, `copy_hardcoded_sequence()`, `bk_compute_merges()`
- Include `types.h` and `<stdint.h>`

**Create `c_backend/src/ToffoliAdditionHelpers.c`:**
- Move `alloc_sequence()` function
- Move `copy_hardcoded_sequence()` function (deep copy with large_control)
- Move `toffoli_sequence_free()` function
- Include: `toffoli_addition_internal.h`, `toffoli_arithmetic_ops.h`, `circuit.h`, `<stdlib.h>`, `<string.h>`

**Create `c_backend/src/ToffoliAdditionCDKM.c`:**
- Move all CDKM-related functions: `emit_MAJ`, `emit_UMA` (uncontrolled); `emit_cMAJ`, `emit_cUMA` (controlled); `emit_CQ_MAJ`, `compute_CQ_layer_count`; `emit_cCQ_MAJ`, `compute_cCQ_layer_count`
- Move main CDKM adder functions: `toffoli_QQ_add`, `toffoli_CQ_add`, `toffoli_cQQ_add`, `toffoli_cCQ_add`
- Move their precompiled static caches (static sequence_t* variables)
- Keep `static` keyword on file-local helpers (emit_MAJ, emit_UMA, etc.)
- Include: `toffoli_addition_internal.h`, `toffoli_arithmetic_ops.h`, `toffoli_sequences.h`, `circuit.h`, `<stdlib.h>`, `<string.h>`, `<stdint.h>`

**Create `c_backend/src/ToffoliAdditionCLA.c`:**
- Move `bk_compute_merges()` function
- Move `bk_cla_ancilla_count()`, `ks_ancilla_count()` (if it exists)
- Move all BK/KS CLA variants: `toffoli_QQ_add_bk`, `toffoli_QQ_add_ks`, `toffoli_CQ_add_bk`, `toffoli_CQ_add_ks`, `toffoli_cQQ_add_bk`, `toffoli_cQQ_add_ks`, `toffoli_cCQ_add_bk`, `toffoli_cCQ_add_ks`
- Move their precompiled static caches
- Include: `toffoli_addition_internal.h`, `toffoli_arithmetic_ops.h`, `toffoli_sequences.h`, `circuit.h`, `<stdlib.h>`, `<string.h>`, `<stdint.h>`

**Delete `c_backend/src/ToffoliAddition.c`** after confirming all code is moved.

CRITICAL: Functions that were `static` in ToffoliAddition.c but are now called from multiple new files (like `alloc_sequence`, `copy_hardcoded_sequence`, `bk_compute_merges`) must be declared in the internal header and have their `static` keyword removed. Functions that are only used within one file keep their `static` keyword.

Verify no function is duplicated across files. Verify all #include ordering is correct (sequences.h must come before any #ifdef SEQ_WIDTH_N guards per project lessons learned).
  </action>
  <verify>
Build with `cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly" && pip3 install -e . 2>&1 | tail -20` -- must compile without errors or warnings about undefined symbols. Then `python3 -m pytest tests/python/ -v --timeout=120 2>&1 | tail -30` -- all existing tests must pass.
  </verify>
  <done>ToffoliAddition.c is replaced by 3 new .c files + 1 internal .h file. All ~1968 lines of code are distributed across the new files with no duplication and no logic changes. Build succeeds. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Toffoli dispatch from hot_path_add.c and update build</name>
  <files>
    c_backend/src/hot_path_add.c
    c_backend/src/hot_path_add_toffoli.c
    setup.py
  </files>
  <action>
Extract the Toffoli-specific CLA/RCA dispatch logic from hot_path_add.c (529 lines) into a separate file. Also update setup.py with all new C files.

**Create `c_backend/src/hot_path_add_toffoli.c`:**
- Extract the Toffoli CLA threshold logic, CLA vs RCA selection, and Toffoli-specific qubit mapping code
- Specifically: the sections that check `circ->cla_override`, call `bk_cla_ancilla_count()`, select between BK/KS variants, and fall back to RCA
- These are the Toffoli-specific code paths within `hot_path_add_qq`, `hot_path_add_cq`, `hot_path_add_cqq`, `hot_path_add_ccq` that handle `arithmetic_mode == ARITH_TOFFOLI`
- Create helper functions like `toffoli_dispatch_qq(circ, bits, qubit_array, invert)` etc. that are called from the main hot_path_add.c functions
- Include: `hot_path_add.h`, `toffoli_arithmetic_ops.h`, `circuit.h`, `qubit_allocator.h`

NOTE: If the Toffoli dispatch logic is deeply interleaved with QFT dispatch in hot_path_add.c (if/else branches rather than clean function boundaries), use Claude's discretion on the cleanest split. The goal is to reduce hot_path_add.c by extracting Toffoli-only logic. If extraction would require excessive interface changes, a less aggressive split is acceptable -- just make sure the Toffoli CLA selection logic is in a clearly identifiable location.

**Update `setup.py`:**
- Remove `ToffoliAddition.c` from C source file list
- Add `ToffoliAdditionCDKM.c`, `ToffoliAdditionCLA.c`, `ToffoliAdditionHelpers.c`
- Add `hot_path_add_toffoli.c`
- All paths relative to `c_backend/src/`
  </action>
  <verify>
Full build: `cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly" && pip3 install -e . 2>&1 | tail -20` -- must compile cleanly. Full test suite: `python3 -m pytest tests/python/ -v --timeout=120 2>&1 | tail -30` -- all 650+ tests pass with zero regressions.
  </verify>
  <done>hot_path_add.c is slimmed down. Toffoli CLA/RCA dispatch logic is in hot_path_add_toffoli.c. setup.py includes all new files and excludes the deleted ToffoliAddition.c. Build compiles cleanly and all tests pass.</done>
</task>

</tasks>

<verification>
1. `ToffoliAddition.c` no longer exists in c_backend/src/
2. Three new files exist: ToffoliAdditionCDKM.c, ToffoliAdditionCLA.c, ToffoliAdditionHelpers.c
3. Internal header toffoli_addition_internal.h exists with shared type definitions
4. hot_path_add_toffoli.c exists with extracted Toffoli dispatch logic
5. setup.py references all new files
6. `pip3 install -e .` compiles without errors
7. `pytest tests/python/ -v` passes all existing tests (650+)
</verification>

<success_criteria>
- ToffoliAddition.c is fully replaced by 3 focused modules (no code left behind)
- Pure refactoring: git diff shows code movement only, zero logic changes
- All 650+ existing tests pass with zero regressions
- Build is clean (no warnings about undefined symbols or missing functions)
</success_criteria>

<output>
After completion, create `.planning/phases/74-mcx-ccx-gate-decomposition-sequence-refactoring/74-01-SUMMARY.md`
</output>

---
phase: 74-mcx-ccx-gate-decomposition-sequence-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - c_backend/include/types.h
  - c_backend/include/gate.h
  - c_backend/src/gate.c
  - c_backend/include/circuit_stats.h
  - c_backend/src/circuit_stats.c
  - c_backend/include/circuit.h
  - c_backend/src/circuit_allocations.c
  - c_backend/src/circuit_output.c
  - src/quantum_language/_core.pxd
  - src/quantum_language/_core.pyx
autonomous: true
requirements: [INF-04]

must_haves:
  truths:
    - "T_GATE and TDG_GATE are valid gate enum values that can be emitted, counted, and exported"
    - "ql.option('toffoli_decompose', True) sets the flag on the circuit; ql.option('toffoli_decompose') returns its value"
    - "gate_counts exposes T and Tdg counts separately, and t_count is computed correctly"
    - "QASM export outputs 't' and 'tdg' instructions for T_GATE and TDG_GATE"
    - "T and Tdg are recognized as inverses of each other"
    - "MCX field removed from gate_counts_t and Python gate_counts dict"
  artifacts:
    - path: "c_backend/include/types.h"
      provides: "T_GATE, TDG_GATE enum values in Standardgate_t"
      contains: "T_GATE"
    - path: "c_backend/src/gate.c"
      provides: "t_gate() and tdg_gate() functions, T/Tdg inverse recognition"
      contains: "t_gate"
    - path: "c_backend/src/circuit_stats.c"
      provides: "T_GATE/TDG_GATE counting, mcx_gates removed, updated t_count logic"
      contains: "T_GATE"
    - path: "c_backend/src/circuit_output.c"
      provides: "QASM export for T and Tdg gates"
      contains: "TDG_GATE"
    - path: "c_backend/include/circuit.h"
      provides: "toffoli_decompose field in circuit_t"
      contains: "toffoli_decompose"
    - path: "src/quantum_language/_core.pyx"
      provides: "toffoli_decompose option API + updated gate_counts dict"
      contains: "toffoli_decompose"
  key_links:
    - from: "c_backend/src/circuit_stats.c"
      to: "c_backend/include/types.h"
      via: "switch on g->Gate"
      pattern: "case T_GATE"
    - from: "c_backend/src/circuit_output.c"
      to: "c_backend/include/types.h"
      via: "switch on g->Gate"
      pattern: "case T_GATE"
    - from: "src/quantum_language/_core.pyx"
      to: "c_backend/include/circuit.h"
      via: "circuit_t field access"
      pattern: "toffoli_decompose"
---

<objective>
Add T_GATE/TDG_GATE gate types to the infrastructure, the `toffoli_decompose` circuit option, updated gate counting (remove MCX, add T/Tdg), QASM export for T/Tdg, and Python API plumbing.

Purpose: Provide the gate-level foundation that MCX decomposition (Plan 03) and CCX->Clifford+T decomposition (Plan 04/05) will build upon. No decomposition logic yet -- just the types, counting, and option flag.
Output: Extended gate enum, new gate primitives, updated stats/QASM, toffoli_decompose option accessible from Python.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-mcx-ccx-gate-decomposition-sequence-refactoring/74-CONTEXT.md
@.planning/phases/74-mcx-ccx-gate-decomposition-sequence-refactoring/74-RESEARCH.md
@c_backend/include/types.h
@c_backend/include/gate.h
@c_backend/src/gate.c
@c_backend/include/circuit_stats.h
@c_backend/src/circuit_stats.c
@c_backend/include/circuit.h
@c_backend/src/circuit_allocations.c
@c_backend/src/circuit_output.c
@src/quantum_language/_core.pxd
@src/quantum_language/_core.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add T_GATE/TDG_GATE enum, gate primitives, inverse recognition, and toffoli_decompose field</name>
  <files>
    c_backend/include/types.h
    c_backend/include/gate.h
    c_backend/src/gate.c
    c_backend/include/circuit.h
    c_backend/src/circuit_allocations.c
  </files>
  <action>
**In `types.h`:**
- Add `T_GATE` and `TDG_GATE` to the `Standardgate_t` enum after `M`:
  `typedef enum { X, Y, Z, R, H, Rx, Ry, Rz, P, M, T_GATE, TDG_GATE } Standardgate_t;`
- Use `T_GATE` not `T` to avoid C macro conflicts (per RESEARCH.md pitfall 3).

**In `gate.h`:**
- Add function declarations:
  `void t_gate(gate_t *g, qubit_t target);`
  `void tdg_gate(gate_t *g, qubit_t target);`

**In `gate.c`:**
- Implement `t_gate()`: Sets g->Gate = T_GATE, g->Target = target, g->NumControls = 0, g->GateValue = M_PI / 4.0
- Implement `tdg_gate()`: Sets g->Gate = TDG_GATE, g->Target = target, g->NumControls = 0, g->GateValue = -M_PI / 4.0
- Update `gates_are_inverse()`: Add cases for T_GATE and TDG_GATE -- T followed by Tdg (or vice versa) on the same qubit are inverses. Both are self-inverse only after 8 applications (not self-inverse), so the case is: (G1->Gate == T_GATE && G2->Gate == TDG_GATE) || (G1->Gate == TDG_GATE && G2->Gate == T_GATE), same target.
- Check all switch statements in gate.c that switch on g->Gate -- add T_GATE/TDG_GATE cases where needed (may fall through to default/other_gates).

**In `circuit.h`:**
- Add `int toffoli_decompose;` field to `circuit_t` struct (after `qubit_saving` field).
- Comment: `// 0 = off (CCX gates in output), 1 = on (decompose CCX to Clifford+T)`

**In `circuit_allocations.c`:**
- Initialize `circ->toffoli_decompose = 0;` in `init_circuit()` (after the `circ->qubit_saving = 0;` line).
  </action>
  <verify>
Build: `cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly" && pip3 install -e . 2>&1 | tail -20` -- must compile without errors or warnings about unhandled enum values.
  </verify>
  <done>T_GATE and TDG_GATE are in the enum. t_gate() and tdg_gate() functions exist. T/Tdg inverse recognition works. toffoli_decompose field exists in circuit_t, initialized to 0. Compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update gate counting, QASM export, and Python option/stats API</name>
  <files>
    c_backend/include/circuit_stats.h
    c_backend/src/circuit_stats.c
    c_backend/src/circuit_output.c
    src/quantum_language/_core.pxd
    src/quantum_language/_core.pyx
  </files>
  <action>
**In `circuit_stats.h`:**
- Update `gate_counts_t` struct:
  - Remove `size_t mcx_gates;` field (MCX will never appear in Toffoli mode output per user decision)
  - Add `size_t t_gates;` (actual T gate count)
  - Add `size_t tdg_gates;` (actual Tdg gate count)
  - Keep `size_t t_count;` but update comment: "Total T-cost: t_gates + tdg_gates when decomposed, or 7 * ccx_gates as estimate when not"
  - Final struct order: x_gates, y_gates, z_gates, h_gates, p_gates, cx_gates, ccx_gates, t_gates, tdg_gates, other_gates, t_count

**In `circuit_stats.c`:**
- In `circuit_gate_counts()` switch statement:
  - Remove the `counts.mcx_gates++` case for X gates with 3+ controls. Instead, route 3+ controls to `counts.other_gates++` (MCX should never appear in Toffoli mode, but if somehow present, count as other)
  - Add cases for T_GATE: `counts.t_gates++; break;`
  - Add cases for TDG_GATE: `counts.tdg_gates++; break;`
- Update t_count formula at end:
  ```c
  if (counts.t_gates > 0 || counts.tdg_gates > 0) {
      // Actual T/Tdg gates present (toffoli_decompose was on)
      counts.t_count = counts.t_gates + counts.tdg_gates;
  } else {
      // Estimate from CCX count (each CCX = 7 T gates)
      counts.t_count = 7 * counts.ccx_gates;
  }
  ```

**In `circuit_output.c`:**
- Find the gate export function (likely `_export_gate` or similar switch on g->Gate)
- Add cases for T_GATE and TDG_GATE:
  - T_GATE: output `"t q[%d];\n"` with g->Target
  - TDG_GATE: output `"tdg q[%d];\n"` with g->Target
- These are standard OpenQASM 2.0 gate names.
- Also check the circuit visualization / print functions -- add T_GATE/TDG_GATE handling there too (may just need a display character).

**In `_core.pxd`:**
- Update the `gate_counts_t` ctypedef:
  - Remove `size_t mcx_gates`
  - Add `size_t t_gates`
  - Add `size_t tdg_gates`
  - Add `int toffoli_decompose` to the `circuit_s` struct declaration (so Cython can access it)

**In `_core.pyx`:**
- In the `option()` function, add a new elif branch for `'toffoli_decompose'`:
  ```python
  elif key == 'toffoli_decompose':
      if value is None:
          return (<circuit_s*><circuit_t*><unsigned long long>_get_circuit()).toffoli_decompose == 1
      if not isinstance(value, bool):
          raise ValueError("toffoli_decompose option requires bool value")
      (<circuit_s*><circuit_t*><unsigned long long>_get_circuit()).toffoli_decompose = 1 if value else 0
  ```
  Place this BEFORE the `else: raise ValueError` catch-all.
- Update the `gate_counts` property dict:
  - Remove `'MCX': counts.mcx_gates`
  - Add `'T_gates': counts.t_gates`
  - Add `'Tdg_gates': counts.tdg_gates`
  - Keep `'T': counts.t_count` (total T-cost)
- Update the option() docstring to document `toffoli_decompose`.
  </action>
  <verify>
Build and test: `cd "/Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum Programming Language/Quantum_Assembly" && pip3 install -e . && python3 -c "import quantum_language as ql; c = ql.circuit(); ql.option('toffoli_decompose', False); print('decompose:', ql.option('toffoli_decompose')); gc = c.gate_counts; print('keys:', sorted(gc.keys())); assert 'MCX' not in gc; assert 'T_gates' in gc; print('PASS')" 2>&1`
Then full test suite: `python3 -m pytest tests/python/ -v --timeout=120 2>&1 | tail -30` -- all tests pass (some tests may reference 'MCX' key in gate_counts and need updating if they assert on that key).

IMPORTANT: If any existing tests reference `gate_counts['MCX']`, update them to remove or replace that assertion. The MCX key is being removed per user decision.
  </verify>
  <done>gate_counts_t has t_gates/tdg_gates instead of mcx_gates. T_GATE/TDG_GATE counted correctly. QASM exports "t" and "tdg". Python ql.option('toffoli_decompose') works. gate_counts dict has T_gates/Tdg_gates keys, no MCX key. All tests pass.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import quantum_language as ql; print(ql.option('toffoli_decompose'))"` returns False
2. `python3 -c "import quantum_language as ql; ql.option('toffoli_decompose', True); print(ql.option('toffoli_decompose'))"` returns True
3. `python3 -c "import quantum_language as ql; c = ql.circuit(); gc = c.gate_counts; print(sorted(gc.keys()))"` shows T_gates, Tdg_gates, T but NOT MCX
4. All 650+ existing tests pass
5. No compiler warnings about unhandled enum values in switch statements
</verification>

<success_criteria>
- T_GATE and TDG_GATE are fully integrated into the gate infrastructure (creation, counting, QASM export, inverse detection)
- toffoli_decompose option is accessible from Python and stored in circuit_t
- gate_counts_t no longer has mcx_gates; has t_gates and tdg_gates instead
- t_count computed correctly (actual when T present, estimate from CCX otherwise)
- Zero test regressions (update any tests referencing MCX key)
</success_criteria>

<output>
After completion, create `.planning/phases/74-mcx-ccx-gate-decomposition-sequence-refactoring/74-02-SUMMARY.md`
</output>

---
phase: 110-merge-verification-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/python/test_merge_equiv.py
autonomous: true
requirements:
  - MERGE-04

must_haves:
  truths:
    - "opt=2 add circuit produces identical statevector to opt=3 for all input pairs at widths 1-4"
    - "opt=2 mul circuit produces identical statevector to opt=3 for all input pairs at widths 1-4"
    - "opt=2 grover oracle circuit produces identical statevector to opt=3"
    - "parametric=True works at opt=1 and opt=3, raises ValueError at opt=2"
  artifacts:
    - path: "tests/python/test_merge_equiv.py"
      provides: "Statevector equivalence tests and parametric+opt interaction tests"
      min_lines: 120
  key_links:
    - from: "tests/python/test_merge_equiv.py"
      to: "quantum_language.compile.CompiledFunc"
      via: "@ql.compile(opt=N) decorator"
      pattern: "ql\\.compile\\(opt="
    - from: "tests/python/test_merge_equiv.py"
      to: "qiskit.quantum_info.Statevector"
      via: "Statevector.from_instruction for equivalence"
      pattern: "Statevector\\.from_instruction"
---

<objective>
Create statevector equivalence tests proving opt=2 merged circuits produce correct results, and verify parametric+opt interaction behavior.

Purpose: Fulfills MERGE-04 by proving merge correctness via Qiskit simulation for arithmetic and grover oracle workloads.
Output: tests/python/test_merge_equiv.py with equivalence and parametric interaction tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-merge-verification-regression/110-CONTEXT.md
@.planning/phases/110-merge-verification-regression/110-RESEARCH.md
@.planning/phases/109-selective-sequence-merging/109-02-SUMMARY.md

@tests/verify_helpers.py
@tests/conftest.py
@src/quantum_language/sim_backend.py

<interfaces>
<!-- Key APIs the executor needs -->

From tests/verify_helpers.py:
```python
def generate_exhaustive_values(width) -> list[int]
def generate_exhaustive_pairs(width) -> list[tuple[int, int]]
```

From src/quantum_language (public API):
```python
ql.circuit()                          # Reset circuit state
ql.qint(value, width=N)              # Create quantum integer
ql.option('fault_tolerant', True)    # Set fault-tolerant backend
ql.to_openqasm()                     # Export to OpenQASM 3.0 string
@ql.compile(opt=N)                   # Compile with optimization level
@ql.compile(parametric=True, opt=N)  # Parametric compilation
```

Statevector extraction (from research):
```python
import qiskit.qasm3
from qiskit.quantum_info import Statevector

qasm_str = ql.to_openqasm()
circuit = qiskit.qasm3.loads(qasm_str)
sv = Statevector.from_instruction(circuit)
# sv.data is numpy array of complex amplitudes, shape (2^n,)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Statevector equivalence tests for add, mul, grover oracle</name>
  <files>tests/python/test_merge_equiv.py</files>
  <behavior>
    - _statevectors_equivalent(sv1_data, sv2_data, atol=1e-10) returns True for identical vectors
    - _statevectors_equivalent returns True for vectors differing only by global phase
    - _statevectors_equivalent returns False for genuinely different vectors
    - test_add_equiv[width=1..4]: opt=2 statevector == opt=3 statevector for all exhaustive input pairs
    - test_mul_equiv[width=1..4]: opt=2 statevector == opt=3 statevector for all exhaustive input pairs
    - test_grover_oracle_equiv: opt=2 statevector == opt=3 statevector for compiled grover oracle (width=3 or 4, predicate x==target)
  </behavior>
  <action>
Create tests/python/test_merge_equiv.py with:

1. Helper function `_statevectors_equivalent(sv1_data, sv2_data, atol=1e-10)`:
   - Find first non-zero amplitude in sv1_data (abs > atol)
   - If none found, check sv2_data is also all zeros
   - Compute global phase factor: sv2_data[idx] / sv1_data[idx]
   - Return np.allclose(sv1_data * phase, sv2_data, atol=atol)

2. Helper function `_get_statevector(build_fn, opt_level)`:
   - Call gc.collect() then ql.circuit()
   - Set ql.option('fault_tolerant', True)
   - Call build_fn with the given opt level (build_fn creates @ql.compile(opt=opt_level) function and invokes it)
   - Export via ql.to_openqasm(), load with qiskit.qasm3.loads(), extract Statevector.from_instruction()
   - Return sv.data numpy array

3. Equivalence tests using generate_exhaustive_pairs from verify_helpers:
   - `test_add_equiv` parametrized by width=[1, 2, 3, 4]: for each (a, b) pair, build add circuit at opt=2 and opt=3, compare statevectors. The build function creates a @ql.compile(opt=N) decorated add function, creates qints with values a and b, calls add.
   - `test_mul_equiv` parametrized by width=[1, 2, 3, 4]: same pattern for multiplication. Width 4 uses 14 qubits (safe). Skip width 5 (17q = limit, too risky with overhead).
   - `test_grover_oracle_equiv`: compile a grover oracle function (predicate: x == 5) at width=4 (7 qubits, safe budget), compare opt=2 vs opt=3 statevectors.

IMPORTANT constraints:
- Max 17 qubits, max_parallel_threads=4 (but Statevector.from_instruction does not need AerSimulator, so thread limit is N/A)
- Always gc.collect() before ql.circuit() to avoid GC pollution
- Compare full statevectors (all qubits including ancilla)
- Use ql.option('fault_tolerant', True) for all tests (per existing patterns)
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && python -m pytest tests/python/test_merge_equiv.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Add equivalence tests pass for all widths (1-4), mul equivalence tests pass for widths 1-4, grover oracle equivalence test passes. All compare opt=2 vs opt=3 statevectors with global phase normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parametric + opt interaction tests</name>
  <files>tests/python/test_merge_equiv.py</files>
  <behavior>
    - test_parametric_opt1_works: parametric=True + opt=1 compiles and produces correct output for 3-4 classical parameter values
    - test_parametric_opt3_works: parametric=True + opt=3 compiles and produces correct output for 3-4 classical parameter values
    - test_parametric_opt2_raises: parametric=True + opt=2 raises ValueError at construction time
  </behavior>
  <action>
Add TestParametricOptInteraction class to tests/python/test_merge_equiv.py:

1. `test_parametric_opt1_works`:
   - gc.collect(), ql.circuit(), ql.option('fault_tolerant', True)
   - Create @ql.compile(parametric=True, opt=1) function that does x += val
   - For val in [1, 3, 5, 7]: create ql.qint(0, width=4), call compiled fn, export QASM, simulate via Statevector.from_instruction(), verify output state has non-zero amplitude at expected index
   - Use width=4 (well within budget)

2. `test_parametric_opt3_works`:
   - Same as opt=1 test but with opt=3
   - Verify output correctness for same parameter values

3. `test_parametric_opt2_raises`:
   - Use pytest.raises(ValueError) context manager
   - Attempt @ql.compile(parametric=True, opt=2) -- should raise at construction time
   - Match pattern "parametric" in error message (or whatever the existing error says)

Note: For parametric tests, topology may differ between opt levels -- only verify output correctness, not circuit structure.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && python -m pytest tests/python/test_merge_equiv.py::TestParametricOptInteraction -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>parametric+opt=1 works with correct results, parametric+opt=3 works with correct results, parametric+opt=2 raises ValueError. All three interaction paths verified.</done>
</task>

</tasks>

<verification>
All equivalence tests pass: `pytest tests/python/test_merge_equiv.py -x -q`
No test exceeds 17 qubits.
</verification>

<success_criteria>
- opt=2 add, mul, and grover oracle circuits produce statevectors equivalent to opt=3
- Exhaustive testing covers all input pairs at widths 1-4 for add and mul
- parametric+opt interaction verified for all three paths (opt=1 works, opt=3 works, opt=2 raises)
- All tests in test_merge_equiv.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/110-merge-verification-regression/110-01-SUMMARY.md`
</output>

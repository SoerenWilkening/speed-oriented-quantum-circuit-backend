---
phase: 60-c-hot-path-migration
plan: 03
type: execute
wave: 3
depends_on: ["60-02"]
files_modified:
  - c_backend/include/hot_path_2.h
  - c_backend/src/hot_path_2.c
  - src/quantum_language/_core.pxd
  - src/quantum_language/qint_arithmetic.pxi
  - src/quantum_language/qint_bitwise.pxi
  - setup.py
  - tests/c/Makefile
  - tests/c/test_hot_path_2.c
autonomous: true

must_haves:
  truths:
    - "Hot path #2 (identified in plan 01) executes entirely in C with single Cython entry point"
    - "All existing tests pass after migration"
    - "Benchmark shows measurable change for migrated path"
  artifacts:
    - path: "c_backend/src/hot_path_2.c"
      provides: "C implementation of hot path #2"
    - path: "c_backend/include/hot_path_2.h"
      provides: "C header for hot path #2"
    - path: "tests/c/test_hot_path_2.c"
      provides: "C-level unit test for hot path #2"
  key_links:
    - from: "src/quantum_language/qint_arithmetic.pxi or qint_bitwise.pxi"
      to: "c_backend/src/hot_path_2.c"
      via: "thin Cython wrapper calling C function"
      pattern: "hot_path_.*\\("
    - from: "setup.py"
      to: "c_backend/src/hot_path_2.c"
      via: "c_sources list"
      pattern: "hot_path_2"
---

<objective>
Migrate hot path #2 (identified in plan 01) from Cython to pure C. Same atomic transition pattern as plan 02: write C, test, integrate, replace, verify.

Purpose: Eliminate Python/C boundary crossing overhead for the #2 hot path.
Output: Hot path #2 fully migrated to C with verified correctness and benchmark data.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-c-hot-path-migration/60-CONTEXT.md
@.planning/phases/60-c-hot-path-migration/60-RESEARCH.md
@.planning/phases/60-c-hot-path-migration/60-01-SUMMARY.md
@.planning/phases/60-c-hot-path-migration/60-02-SUMMARY.md
@src/quantum_language/_core.pxd
@src/quantum_language/qint_arithmetic.pxi
@src/quantum_language/qint_bitwise.pxi
@c_backend/include/execution.h
@c_backend/include/arithmetic_ops.h
@c_backend/include/bitwise_ops.h
@setup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write C implementation and unit test for hot path #2</name>
  <files>c_backend/include/hot_path_2.h, c_backend/src/hot_path_2.c, tests/c/test_hot_path_2.c, tests/c/Makefile</files>
  <action>
  Read 60-01-SUMMARY.md to determine which operation is hot path #2.

  Follow the EXACT same pattern as plan 02 Task 1:

  1. Create `c_backend/include/hot_path_2.h` with C function declaration.
     - File name should reflect the actual operation (e.g., hot_path_ixor.h)
     - Signature follows the same pattern: circuit_t*, qubit arrays, bits, control state
     - Adjust parameters to match the specific operation's needs

  2. Create `c_backend/src/hot_path_2.c` implementing the function.
     - CRITICAL: Match the exact qubit array layout from the Cython code being replaced
     - Handle controlled/uncontrolled variants
     - Handle QQ and CQ paths as applicable
     - Use existing C backend functions (Q_xor, QQ_add, CQ_add, etc.) -- do NOT reimplement gate generation
     - Build qubit array on stack, call sequence generator, call run_instruction

  3. Create `tests/c/test_hot_path_2.c`:
     - Test function produces gates in circuit
     - Test controlled and uncontrolled variants
     - Test multiple bit widths (4, 8, 16)
     - Pattern: init_circuit -> call function -> assert circuit has gates -> free_circuit

  4. Update `tests/c/Makefile` with new test target.
     - Include all required C backend sources for linking
  </action>
  <verify>
  - C test compiles and passes: `make -C tests/c test_hot_path_2 && ./tests/c/test_hot_path_2`
  - No compiler warnings with -Wall -Wextra
  </verify>
  <done>
  C implementation and unit test for hot path #2 exist, compile, and pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate hot path #2 and replace Cython code</name>
  <files>setup.py, src/quantum_language/_core.pxd, src/quantum_language/qint_arithmetic.pxi or qint_bitwise.pxi</files>
  <action>
  Follow the EXACT same pattern as plan 02 Task 2:

  1. Add new C file to setup.py c_sources list.

  2. Add cdef extern declaration to _core.pxd with `nogil` annotation.
     - Use appropriate header name for this hot path

  3. Replace the Cython implementation in the relevant .pxi file:
     - Keep Python-facing method signature identical
     - Extract all Python data before the C call
     - Build C arrays on Cython stack
     - Wrap C function call in `with nogil:` block (CYT-04)
     - Return appropriate value (self for in-place, new object for out-of-place)

  4. Rebuild: `pip install -e .`

  5. Run full test suite: `pytest tests/python/ -v`
     - ALL tests must pass. If any fail, the qubit layout is wrong -- compare with the original Cython code line by line.

  6. Run benchmarks: `pytest tests/benchmarks/test_qint_benchmark.py -v --benchmark-only`
  </action>
  <verify>
  - `pip install -e .` compiles without errors
  - `pytest tests/python/ -v` -- ALL tests pass
  - Benchmarks run and capture timing data
  </verify>
  <done>
  Hot path #2 fully migrated: C function called via thin Cython wrapper with nogil, all tests pass, benchmark data captured.
  </done>
</task>

</tasks>

<verification>
- C unit test compiles and passes
- Full Python test suite passes (no regressions)
- Benchmark data captured for migrated path
- Two hot paths now migrated to C (cumulative with plan 02)
</verification>

<success_criteria>
1. Hot path #2 gate generation occurs entirely in C
2. All existing tests pass without modification
3. nogil applied around the C function call (CYT-04)
4. Benchmark timing captured for before/after comparison
</success_criteria>

<output>
After completion, create `.planning/phases/60-c-hot-path-migration/60-03-SUMMARY.md`

Include:
- Which hot path was migrated
- Before/after benchmark numbers
- Files created/modified
- Cumulative improvement so far (paths 1 + 2)
</output>

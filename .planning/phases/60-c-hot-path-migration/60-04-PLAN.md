---
phase: 60-c-hot-path-migration
plan: 04
type: execute
wave: 4
depends_on: ["60-03"]
files_modified:
  - c_backend/include/hot_path_3.h
  - c_backend/src/hot_path_3.c
  - src/quantum_language/_core.pxd
  - src/quantum_language/qint_arithmetic.pxi
  - src/quantum_language/qint_bitwise.pxi
  - setup.py
  - tests/c/Makefile
  - tests/c/test_hot_path_3.c
  - tests/benchmarks/test_qint_benchmark.py
autonomous: true

must_haves:
  truths:
    - "Hot path #3 (identified in plan 01) executes entirely in C with single Cython entry point"
    - "All existing tests pass after all 3 migrations"
    - "Aggregate or per-operation benchmark improvement is measurable (target >20%)"
    - "All 3 migrated hot paths use nogil (CYT-04) around their C calls"
  artifacts:
    - path: "c_backend/src/hot_path_3.c"
      provides: "C implementation of hot path #3"
    - path: "c_backend/include/hot_path_3.h"
      provides: "C header for hot path #3"
    - path: "tests/c/test_hot_path_3.c"
      provides: "C-level unit test for hot path #3"
  key_links:
    - from: "src/quantum_language/qint_arithmetic.pxi or qint_bitwise.pxi"
      to: "c_backend/src/hot_path_3.c"
      via: "thin Cython wrapper calling C function"
      pattern: "hot_path_.*\\("
    - from: "tests/benchmarks/test_qint_benchmark.py"
      to: "benchmark comparison"
      via: "final benchmark run"
      pattern: "TestHotPath"
---

<objective>
Migrate hot path #3 (identified in plan 01) to C, then run final comprehensive benchmarks comparing all 3 migrated paths against the plan 01 baseline.

Purpose: Complete the phase by migrating the third hot path and validating the aggregate performance improvement.
Output: All 3 hot paths migrated. Final benchmark comparison documenting improvement.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-c-hot-path-migration/60-CONTEXT.md
@.planning/phases/60-c-hot-path-migration/60-RESEARCH.md
@.planning/phases/60-c-hot-path-migration/60-01-SUMMARY.md
@.planning/phases/60-c-hot-path-migration/60-02-SUMMARY.md
@.planning/phases/60-c-hot-path-migration/60-03-SUMMARY.md
@src/quantum_language/_core.pxd
@src/quantum_language/qint_arithmetic.pxi
@src/quantum_language/qint_bitwise.pxi
@c_backend/include/execution.h
@setup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate hot path #3 to C (write, test, integrate, replace)</name>
  <files>c_backend/include/hot_path_3.h, c_backend/src/hot_path_3.c, tests/c/test_hot_path_3.c, tests/c/Makefile, setup.py, src/quantum_language/_core.pxd, src/quantum_language/qint_arithmetic.pxi or qint_bitwise.pxi</files>
  <action>
  Read 60-01-SUMMARY.md to determine which operation is hot path #3.

  Complete the FULL migration cycle in one task (since the pattern is now established from plans 02-03):

  1. **Write C implementation:**
     - Create `c_backend/include/hot_path_3.h` with function declaration
     - Create `c_backend/src/hot_path_3.c` with implementation
     - Match exact qubit array layout from Cython code
     - Handle controlled/uncontrolled and QQ/CQ variants as applicable
     - Use existing C sequence generators (do not reimplement)

  2. **Write C unit test:**
     - Create `tests/c/test_hot_path_3.c`
     - Test basic operation, controlled variant, multiple widths
     - Update `tests/c/Makefile`
     - Verify: compile and run passes

  3. **Integrate into build:**
     - Add to setup.py c_sources
     - Add cdef extern to _core.pxd with `nogil`

  4. **Replace Cython code:**
     - Replace implementation in .pxi file with thin wrapper
     - Extract Python data, call C function with `with nogil:`, return
     - Keep Python-facing API identical

  5. **Verify:**
     - `pip install -e .` succeeds
     - `pytest tests/python/ -v` -- ALL tests pass
     - C test compiles and passes
  </action>
  <verify>
  - C unit test compiles and passes
  - `pip install -e .` builds without errors
  - `pytest tests/python/ -v` -- ALL tests pass (no regressions from any of the 3 migrations)
  </verify>
  <done>
  Hot path #3 fully migrated to C. All 3 hot paths now execute in C with nogil.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final benchmarks and phase validation</name>
  <files>tests/benchmarks/test_qint_benchmark.py</files>
  <action>
  1. Add a `TestHotPathMigrated` class to test_qint_benchmark.py (if not already added in plan 01) with before/after comparison tests for all 3 migrated paths:
     ```python
     class TestHotPathMigrated:
         """Post-migration benchmarks for Phase 60 comparison."""

         def test_migrated_path_1(self, benchmark, clean_circuit):
             # Use same pattern as baseline tests from plan 01
             ...

         def test_migrated_path_2(self, benchmark, clean_circuit):
             ...

         def test_migrated_path_3(self, benchmark, clean_circuit):
             ...
     ```
     (Only add tests that don't already exist -- the existing benchmark suite may already cover these operations.)

  2. Run final comprehensive benchmarks:
     ```bash
     pytest tests/benchmarks/test_qint_benchmark.py -v --benchmark-only
     ```

  3. Compare results against plan 01 baseline:
     - Per-path: old_mean vs new_mean for each migrated operation
     - Aggregate: compare total across all benchmarked operations
     - Calculate percentage improvement

  4. Run full test suite one final time to confirm no regressions across the entire codebase:
     ```bash
     pytest tests/python/ -v
     ```

  5. Verify CYT-04 (nogil) is correctly applied on all 3 migrated paths:
     - Check each .pxi file for `with nogil:` around the C function call
     - Verify `nogil` annotation on each cdef extern declaration in _core.pxd

  6. Document results:
     - Create before/after comparison table
     - Note whether >20% improvement was achieved (per-operation or aggregate)
     - If <20%, document why (e.g., overhead dominated by run_instruction malloc, not boundary crossings)
  </action>
  <verify>
  - All benchmarks run successfully
  - All Python tests pass
  - Before/after comparison computed
  - CYT-04 nogil confirmed on all 3 paths
  </verify>
  <done>
  All 3 hot paths migrated. Final benchmarks captured. Performance comparison documented. Phase 60 success criteria evaluated: either per-operation or aggregate >20% improvement achieved (or documented reason for gap).
  </done>
</task>

</tasks>

<verification>
- All 3 C unit tests compile and pass
- Full Python test suite passes with all 3 migrations active
- Before/after benchmark comparison shows results
- nogil applied on all 3 migrated paths
- Phase success criteria evaluated
</verification>

<success_criteria>
1. All 3 hot paths migrated to C with separate .c/.h files per path
2. All existing tests pass after all migrations
3. nogil (CYT-04) applied on all migrated Cython wrappers
4. Benchmark shows >20% improvement per-operation or aggregate (or documented reason)
5. Final comparison table in summary
</success_criteria>

<output>
After completion, create `.planning/phases/60-c-hot-path-migration/60-04-SUMMARY.md`

Include:
- Which hot path #3 was migrated
- Final before/after benchmark comparison table (ALL 3 paths)
- Whether >20% improvement was achieved
- CYT-04 nogil confirmation
- Complete file inventory of phase deliverables
</output>

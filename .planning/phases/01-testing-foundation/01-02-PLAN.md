---
phase: 01-testing-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/python/test_qint_operations.py
  - tests/python/test_qbool_operations.py
  - tests/python/test_circuit_generation.py
autonomous: true

must_haves:
  truths:
    - "Characterization tests capture current behavior of all qint arithmetic operations (+, -, *, +=, -=)"
    - "Characterization tests capture current behavior of all qint comparison operations (<, <=, >, >=)"
    - "Characterization tests capture current behavior of all qint/qbool logic operations (&, |, ~)"
    - "Characterization tests capture context manager (with statement) behavior"
    - "Tests pass on current codebase without modifications"
    - "Running pytest shows all tests with names, pass/fail status, and timing"
  artifacts:
    - path: "tests/python/test_qint_operations.py"
      provides: "Characterization tests for qint arithmetic and comparison"
      min_lines: 100
    - path: "tests/python/test_qbool_operations.py"
      provides: "Characterization tests for qbool logic operations"
      min_lines: 50
    - path: "tests/python/test_circuit_generation.py"
      provides: "Characterization tests for circuit structure and output"
      min_lines: 30
  key_links:
    - from: "tests/python/test_qint_operations.py"
      to: "python-backend/quantum_language.pyx"
      via: "import quantum_language"
      pattern: "import quantum_language"
    - from: "tests/python/conftest.py"
      to: "tests/python/test_*.py"
      via: "pytest fixtures"
      pattern: "clean_circuit|sample_qints"
---

<objective>
Create comprehensive characterization tests that capture the current behavior of all qint and qbool operations before any refactoring begins.

Purpose: Document existing behavior as golden masters so that future refactoring can be validated against known-working state. Tests should pass on the current codebase even if there are bugs - we're capturing what EXISTS, not what SHOULD exist.

Output: tests/python/test_qint_operations.py, tests/python/test_qbool_operations.py, tests/python/test_circuit_generation.py
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-foundation/01-CONTEXT.md
@.planning/phases/01-testing-foundation/01-RESEARCH.md
@.planning/phases/01-testing-foundation/01-01-SUMMARY.md
@python-backend/quantum_language.pyx
@python-backend/test.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create qint arithmetic characterization tests</name>
  <files>tests/python/test_qint_operations.py</files>
  <action>
Create tests/python/test_qint_operations.py with characterization tests for all qint operations:

1. Import setup:
   - Add python-backend to sys.path
   - import quantum_language as ql
   - import pytest

2. Test qint creation:
   - test_qint_creation_default(): Create qint(), verify it exists and has default bits
   - test_qint_creation_with_value(): Create qint(value=5, bits=8), verify initialization
   - test_qint_creation_various_widths(): Parametrized test for bits=2,4,8,16

3. Test arithmetic operations (capture circuit generation, not numerical results):
   - test_qint_addition_inplace(): a = ql.qint(bits=8); a += 5; verify circuit generated
   - test_qint_addition_outofplace(): a = ql.qint(bits=8); b = a + 5; verify b exists
   - test_qint_addition_qint_qint(): a + b where both are qints
   - test_qint_subtraction_inplace(): a -= 5
   - test_qint_subtraction_outofplace(): b = a - 5
   - test_qint_multiplication(): a * 5 and a * b

4. Test comparison operations:
   - test_qint_less_than(): a < 5 returns qbool
   - test_qint_less_equal(): a <= 5 returns qbool
   - test_qint_greater_than(): a > 5 returns qbool
   - test_qint_greater_equal(): a >= 5 returns qbool

5. For each test, capture that:
   - Operation completes without exception
   - Result type is correct (qint or qbool as appropriate)
   - Circuit has been modified (gate count > 0 after operation)

Note: We're testing that operations WORK, not that they produce specific gate sequences. The goal is to catch regressions during refactoring.
  </action>
  <verify>
cd /Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum\ Programming\ Language/Quantum_Assembly && source .venv/bin/activate && python -m pytest tests/python/test_qint_operations.py -v
  </verify>
  <done>test_qint_operations.py contains tests for all arithmetic and comparison operations, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create qbool and logic operations characterization tests</name>
  <files>tests/python/test_qbool_operations.py</files>
  <action>
Create tests/python/test_qbool_operations.py with characterization tests for qbool and logic operations:

1. Import setup (same as test_qint_operations.py)

2. Test qbool creation:
   - test_qbool_creation(): Create qbool(), verify exists
   - test_qbool_from_qint_bit(): Access qint[i] returns qbool-like object

3. Test logic operations:
   - test_qbool_and(): a & b where a, b are qbools
   - test_qbool_or(): a | b
   - test_qbool_invert(): ~a (NOT operation)
   - test_qint_and_qint(): qint & qint logic

4. Test context manager (controlled operations):
   - test_context_manager_with_qbool():
     ```python
     a = ql.qbool()
     b = ql.qint(bits=4)
     with a:
         b += 1
     ```
     Verify circuit contains controlled operation
   - test_nested_context_manager():
     ```python
     a = ql.qbool()
     c = ql.qbool()
     b = ql.qint(bits=4)
     with a & c:
         b += 1
     ```

5. Test array creation:
   - test_array_1d_qint(): ql.array(3, dtype=ql.qint)
   - test_array_2d_qbool(): ql.array((3,3), dtype=ql.qbool)
   - test_array_from_list(): ql.array([1,2,3], dtype=ql.qint)

Note: Tests should verify operations complete and produce expected types, not specific gate sequences.
  </action>
  <verify>
cd /Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum\ Programming\ Language/Quantum_Assembly && source .venv/bin/activate && python -m pytest tests/python/test_qbool_operations.py -v
  </verify>
  <done>test_qbool_operations.py contains tests for all logic operations and context manager, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create circuit generation characterization tests</name>
  <files>tests/python/test_circuit_generation.py</files>
  <action>
Create tests/python/test_circuit_generation.py with characterization tests for circuit output:

1. Import setup (same as other test files)
   - Also import io and contextlib for capturing stdout

2. Test circuit output capture:
   - test_print_circuit_produces_output():
     - Create qint, perform operation
     - Capture stdout from qint.print_circuit()
     - Verify output contains "cycles" and "gates" (based on gate.c output format)

3. Test circuit structure:
   - test_circuit_initialization(): Verify circuit initializes on first qint creation
   - test_multiple_qints_same_circuit(): Multiple qints use same underlying circuit

4. Test circuit reset behavior:
   - Document whether creating new qints reuses or extends circuit
   - This is characterization - capture WHAT happens, not WHAT SHOULD happen

5. Integration-style characterization (from existing test.py pattern):
   - test_tic_tac_toe_pattern(): Simplified version of test.py logic
     - Create arrays, perform conditional operations
     - Verify circuit generates without error

Note: Use normalize_circuit_output() from conftest.py to remove non-deterministic elements before any comparisons.
  </action>
  <verify>
cd /Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum\ Programming\ Language/Quantum_Assembly && source .venv/bin/activate && python -m pytest tests/python/test_circuit_generation.py -v
  </verify>
  <done>test_circuit_generation.py contains tests for circuit output and structure, all tests pass</done>
</task>

</tasks>

<verification>
1. Run full test suite: cd /Users/sorenwilkening/Desktop/UNI/Promotion/Projects/Quantum\ Programming\ Language/Quantum_Assembly && source .venv/bin/activate && python -m pytest tests/python/ -v
2. Verify test count: Should have at least 20 characterization tests total
3. Verify all tests pass: Exit code 0, no failures
4. Check test timing is reported: Output shows per-test timing
</verification>

<success_criteria>
- tests/python/test_qint_operations.py has tests for: +, -, *, +=, -=, <, <=, >, >=
- tests/python/test_qbool_operations.py has tests for: &, |, ~, context manager, array
- tests/python/test_circuit_generation.py has tests for: print_circuit output, circuit initialization
- All tests pass on current codebase (pytest exit code 0)
- Tests are verbose with clear names describing what behavior they capture
- Test output shows timing information
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-02-SUMMARY.md`
</output>

---
phase: 29-c-backend-bug-fixes
plan: 14
type: execute
wave: 2
depends_on: [13]
files_modified:
  - c_backend/src/IntegerMultiplication.c
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Multiplication of non-trivial operands (2*3, 2*2, 3*3) returns correct results through the verification pipeline"
    - "No regressions in BUG-01, BUG-02, or BUG-04 test suites"
  artifacts:
    - path: "c_backend/src/IntegerMultiplication.c"
      provides: "Corrected QQ_mul algorithm producing correct products"
  key_links:
    - from: "c_backend/src/IntegerMultiplication.c"
      to: "tests/bugfix/test_bug03_multiplication.py"
      via: "OpenQASM export -> Qiskit simulate -> result check"
---

<objective>
Fix QQ_mul non-trivial multiplication results if plan 29-13 verification shows they still fail.

Purpose: Plan 29-13 rebuilds and tests the existing target qubit fixes. If non-trivial QQ_mul products (2*3, 2*2, 3*3) still return wrong values, this plan performs deeper algorithm investigation and applies corrective changes. If 29-13 shows multiplication passes, this plan can be skipped.

Output: Either corrected QQ_mul producing right products, or documented evidence that the issue requires BUG-05 fix first.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-c-backend-bug-fixes/29-13-SUMMARY.md
@.planning/phases/29-c-backend-bug-fixes/29-11-SUMMARY.md
@c_backend/src/IntegerMultiplication.c
@c_backend/src/IntegerAddition.c (reference — QQ_add works correctly, similar Draper adder pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze QQ_mul algorithm against working CQ_mul and QQ_add patterns</name>
  <files>c_backend/src/IntegerMultiplication.c, c_backend/src/IntegerAddition.c</files>
  <action>
Read 29-13-SUMMARY.md to see which multiplication tests pass/fail. If the file does not exist, FAIL with an error: "29-13-SUMMARY.md missing — Plan 13 must be executed first." If the file exists and all multiplication tests pass, SKIP this plan entirely and create a summary saying "Resolved by 29-13."

If non-trivial products still fail:

1. Compare the working CQ_mul (which passes 10/10 tests per 29-11) with QQ_mul to identify structural differences.

2. Compare working QQ_add (which passes 7/7 tests per 29-12) with QQ_mul's Draper adder invocations. QQ_mul essentially does repeated controlled additions, so its inner loop should follow the same pattern as QQ_add.

3. Check QQ_mul Block structure:
   - Block 1 (lines ~200-215): Initial controlled phase rotations
   - Block 2 (lines ~216-240): Culmination rotations — already has target fix from working tree
   - Block 3 (lines ~241+): Controlled-controlled phase operations (CCP decomposition)

   For each block, verify:
   - Control qubit indices match the QQ_add convention (3*bits - bit_int2 - 1)
   - Target qubit indices now use `i` (from working tree fix)
   - Phase rotation values match the mathematical formula for multiplication

4. Look specifically at whether the issue is:
   a. Phase rotation VALUES (the mathematical formulas for the multiplier effect)
   b. CONTROL qubit mapping (which qubits are controls in the CCP gates)
   c. Gate ORDERING (whether gates need to be applied in a specific order within each block)
   d. BUG-05 cache pollution (non-deterministic results indicate this)

5. If the 2*3 test is non-deterministic (got 13 then 15 in 29-12), that strongly points to BUG-05 cache contamination. In that case, test with a fresh Python process per test:
   ```bash
   python3 -c "
   from quantum_language.qint import qint
   from quantum_language.circuit import circuit
   def test_mul(a, b, bits):
       x = qint(a, width=bits)
       y = qint(b, width=bits)
       z = x * y
       c = circuit()
       qasm = c.openqasm()
       # export and simulate
       ...
   test_mul(2, 3, 4)
   "
   ```
   If isolated tests give different results than pytest suite, BUG-05 is confirmed as the blocker.
  </action>
  <verify>
   Analysis documents the root cause of remaining QQ_mul failures with specific evidence.
  </verify>
  <done>Root cause identified: either (a) specific code fix applied and tested, or (b) documented that BUG-05 cache pollution is the blocker, with evidence from isolated vs suite test comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fix and verify (if root cause is fixable)</name>
  <files>c_backend/src/IntegerMultiplication.c</files>
  <action>
If Task 1 identifies a fixable issue (not BUG-05):

1. Apply the fix to IntegerMultiplication.c
2. Rebuild: `rm -f src/quantum_language/qint.c && pip install --break-system-packages -e . --no-build-isolation`
3. Run multiplication tests:
   ```bash
   python3 -m pytest tests/bugfix/test_bug03_multiplication.py -xvs
   ```
4. Run regression check on other suites:
   ```bash
   python3 -m pytest tests/bugfix/test_bug01_subtraction.py tests/bugfix/test_bug04_qft_addition.py tests/bugfix/test_bug02_comparison.py -v
   ```

If Task 1 concludes BUG-05 is the blocker:
1. Document this finding
2. Run isolated single-test processes to confirm deterministic correctness when cache is fresh
3. If isolated tests pass, document that QQ_mul is algorithmically correct but BUG-05 prevents suite-level verification
4. Assess whether phase 29 criterion 3 can be considered "conditionally met" (correct when cache is clean)
  </action>
  <verify>
   If fix applied: `python3 -m pytest tests/bugfix/test_bug03_multiplication.py -xvs` shows >= 4/5 pass.
   If BUG-05 blocker: isolated process tests documented with results.
   No regressions in BUG-01, BUG-02, BUG-04.
  </verify>
  <done>Either multiplication tests pass (criterion 3 met) or BUG-05 documented as the remaining blocker with evidence that the algorithm is correct in isolation.</done>
</task>

</tasks>

<verification>
1. If fix applied: `python3 -m pytest tests/bugfix/ -v` shows >= 16/19 tests pass
2. No regressions: BUG-01 (5/5), BUG-02 (improved from 29-13), BUG-04 (7/7)
3. Phase 29 success criteria re-evaluated: target 4/5 or 5/5
</verification>

<success_criteria>
- QQ_mul non-trivial products either fixed or root cause documented with evidence
- No regressions in other bug fix suites
- Phase 29 overall score improves or blocker is clearly identified for escalation
</success_criteria>

<output>
After completion, create `.planning/phases/29-c-backend-bug-fixes/29-14-SUMMARY.md`
</output>

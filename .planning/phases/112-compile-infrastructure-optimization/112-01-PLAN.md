---
phase: 112-compile-infrastructure-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/python/test_compile_performance.py
  - src/quantum_language/call_graph.py
autonomous: true
requirements:
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "Profiling benchmarks capture baseline timing for qubit_set construction and overlap detection before numpy migration"
    - "DAGNode stores numpy array alongside frozenset for fast overlap computation"
    - "Overlap methods (build_overlap_edges, parallel_groups, merge_groups) use np.intersect1d instead of frozenset intersection"
    - "All 76 call_graph tests and 29 merge tests pass with zero regressions"
  artifacts:
    - path: "tests/python/test_compile_performance.py"
      provides: "Qubit set construction and overlap detection micro-benchmarks"
      contains: "bench_qubit_set_construction"
    - path: "src/quantum_language/call_graph.py"
      provides: "DAGNode with _qubit_array and numpy overlap detection"
      contains: "_qubit_array"
  key_links:
    - from: "src/quantum_language/call_graph.py"
      to: "numpy"
      via: "np.intersect1d in overlap methods"
      pattern: "np\\.intersect1d"
    - from: "src/quantum_language/call_graph.py"
      to: "DAGNode.__init__"
      via: "_qubit_array stored alongside qubit_set"
      pattern: "_qubit_array"
---

<objective>
Establish profiling baseline for COMP-03 and migrate call_graph.py overlap computation to numpy (COMP-02).

Purpose: Capture before-migration timing data and convert the DAGNode + overlap methods to use numpy arrays for pairwise qubit overlap detection, while preserving frozenset backward compatibility for all consumers.

Output: Extended benchmarks in test_compile_performance.py, numpy-based call_graph.py with dual storage DAGNode.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-compile-infrastructure-optimization/112-RESEARCH.md

@src/quantum_language/call_graph.py
@tests/python/test_compile_performance.py
@tests/python/test_call_graph.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend profiling benchmarks with qubit_set and overlap micro-benchmarks (COMP-03 baseline)</name>
  <files>tests/python/test_compile_performance.py</files>
  <action>
Add two new benchmark tests to test_compile_performance.py to capture baseline timing before numpy migration:

1. `test_qubit_set_construction_benchmark` (marked @pytest.mark.benchmark):
   - Create a helper `_bench_qubit_set_construction(n_functions, qubits_per_func, qubit_range)` that simulates what compile.py does: for each function, build a set() from multiple quantum arg qubit index lists using set.update(), then convert to frozenset.
   - Use `n_functions=50, qubits_per_func=10, qubit_range=100` as the "large" case (representative of chess walk).
   - Use `n_functions=10, qubits_per_func=3, qubit_range=20` as the "small" case.
   - Measure with time.perf_counter_ns(), run 20 iterations, report median in microseconds.
   - Print results in the same format as existing benchmarks.

2. `test_overlap_detection_benchmark` (marked @pytest.mark.benchmark):
   - Create a helper `_bench_overlap_detection(n_nodes, qubit_range)` that creates a CallGraphDAG with n_nodes nodes having random qubit sets (use numpy.random.default_rng(42) for determinism), then times `build_overlap_edges()`.
   - Test with n_nodes=20, qubit_range=50 (medium) and n_nodes=50, qubit_range=100 (large).
   - Measure with time.perf_counter_ns(), run 10 iterations, report median in microseconds.

Import numpy and CallGraphDAG at top of file. The benchmark functions should print clearly labeled "BASELINE" in output for later comparison.

Run the benchmarks once to capture baseline numbers (output will be in test stdout).
  </action>
  <verify>
    <automated>pytest tests/python/test_compile_performance.py -v -m benchmark -s 2>&1 | head -60</automated>
  </verify>
  <done>Two new benchmark tests exist and run successfully, printing baseline timing for qubit_set construction and overlap detection</done>
</task>

<task type="auto">
  <name>Task 2: Migrate call_graph.py to numpy overlap detection with dual-storage DAGNode (COMP-02)</name>
  <files>src/quantum_language/call_graph.py</files>
  <action>
Migrate call_graph.py to use numpy for overlap computation while preserving frozenset backward compatibility:

1. Add `import numpy as np` at top of file (after rustworkx import).

2. DAGNode.__init__ -- add `_qubit_array` to __slots__ and initialize it:
   - Add `"_qubit_array"` to the __slots__ tuple.
   - After `self.qubit_set = frozenset(qubit_set)`, add: `self._qubit_array = np.array(sorted(self.qubit_set), dtype=np.intp)`
   - Keep the bitmask computation using Python int loop (do NOT convert to numpy -- >64 qubit support requires Python arbitrary precision ints).

3. build_overlap_edges method -- replace frozenset intersection with np.intersect1d:
   - Line ~211: Replace `w = len(qs_i & self._nodes[j].qubit_set)` with `w = len(np.intersect1d(arr_i, self._nodes[j]._qubit_array))`
   - Line ~209: Change `qs_i = self._nodes[i].qubit_set` to `arr_i = self._nodes[i]._qubit_array`

4. parallel_groups method -- same pattern:
   - Line ~238: Replace `qs_i = self._nodes[i].qubit_set` with `arr_i = self._nodes[i]._qubit_array`
   - Line ~240: Replace `w = len(qs_i & self._nodes[j].qubit_set)` with `w = len(np.intersect1d(arr_i, self._nodes[j]._qubit_array))`

5. merge_groups method -- same pattern:
   - Line ~272: Replace `qs_i = self._nodes[i].qubit_set` with `arr_i = self._nodes[i]._qubit_array`
   - Line ~274: Replace `w = len(qs_i & self._nodes[j].qubit_set)` with `w = len(np.intersect1d(arr_i, self._nodes[j]._qubit_array))`

6. Update the module docstring line about overlap computation: change "frozenset intersection" to "numpy intersect1d on sorted arrays".

7. In compile.py (line ~1160), the capture finalization path rebuilds node attributes including qubit_set. After `node.qubit_set = frozenset(qubit_set)`, add: `node._qubit_array = np.array(sorted(node.qubit_set), dtype=np.intp)` and after `node.bitmask = bitmask` (line ~1172). This ensures the finalization path also updates the numpy array. Note: compile.py already imports numpy, so no new import needed.

IMPORTANT: Do NOT remove or change the qubit_set frozenset attribute -- tests and external code depend on it. The _qubit_array is supplementary.
  </action>
  <verify>
    <automated>pytest tests/python/test_call_graph.py tests/python/test_merge.py -x -q</automated>
  </verify>
  <done>All 76 call_graph tests and 29 merge tests pass. DAGNode stores _qubit_array. Overlap methods use np.intersect1d.</done>
</task>

</tasks>

<verification>
- `pytest tests/python/test_call_graph.py tests/python/test_merge.py -x -q` -- all pass, zero regressions
- `pytest tests/python/test_compile_performance.py -v -m benchmark -s` -- benchmarks run and print timing data
- `python -c "from quantum_language.call_graph import DAGNode; n = DAGNode('f', {1,2,3}, 10, (1,)); print(n._qubit_array)"` -- prints numpy array [1 2 3]
</verification>

<success_criteria>
- DAGNode has _qubit_array attribute (numpy array, dtype=np.intp) alongside frozenset qubit_set
- build_overlap_edges, parallel_groups, merge_groups all use np.intersect1d
- Profiling baseline benchmarks exist and produce timing data
- Zero test regressions (76 call_graph + 29 merge tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/112-compile-infrastructure-optimization/112-01-SUMMARY.md`
</output>

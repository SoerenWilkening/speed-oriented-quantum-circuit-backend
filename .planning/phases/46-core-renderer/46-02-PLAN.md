---
phase: 46-core-renderer
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - src/quantum_language/draw.py
  - tests/test_draw_render.py
autonomous: true

must_haves:
  truths:
    - "CNOT gates show a vertical control line from control qubit to target qubit"
    - "CCX/MCX gates show vertical control lines spanning all involved qubits"
    - "Control dots appear as bright pixels at each control qubit position"
    - "Control lines render in a distinct bright color separate from wire color"
    - "Gate blocks overlay control lines correctly (rendered after control lines)"
    - "Control dots remain visible on top of gate blocks (rendered last)"
  artifacts:
    - path: "src/quantum_language/draw.py"
      provides: "Complete renderer with control line and control dot rendering"
      contains: "CTRL_COLOR"
    - path: "tests/test_draw_render.py"
      provides: "Tests for multi-qubit gate rendering"
      contains: "test_cnot"
  key_links:
    - from: "src/quantum_language/draw.py"
      to: "gate['controls'] list in draw_data"
      via: "iterates controls to draw vertical lines and dots"
      pattern: "gate\\['controls'\\]"
---

<objective>
Add multi-qubit gate rendering to the draw.py renderer: vertical control lines between control and target qubits, control dots at control positions, and correct rendering order (control lines before gate blocks, control dots after). Add tests for CNOT, CCX, and MCX gate visualization.

Purpose: Multi-qubit gates (CNOT, CCX, MCX) are fundamental to quantum circuits. Without control line visualization, users cannot see qubit entanglement structure.
Output: Updated draw.py with complete multi-qubit rendering, tests for control lines and dots.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-core-renderer/46-CONTEXT.md
@.planning/phases/46-core-renderer/46-RESEARCH.md
@.planning/phases/46-core-renderer/46-01-SUMMARY.md
@src/quantum_language/draw.py
@tests/test_draw_render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement control line and control dot rendering in draw.py</name>
  <files>src/quantum_language/draw.py</files>
  <action>
Update the `render()` function in draw.py to add control line and control dot rendering. The rendering order must be:

1. Background fill (already done in plan 01)
2. Horizontal wires (already done in plan 01)
3. **NEW: Vertical control lines** -- After wires, before gate blocks. For each gate with non-empty `controls`:
   - Compute `gx = gate['layer'] * CELL_W`
   - Collect all involved qubits: `[gate['target']] + gate['controls']`
   - Compute `y_min = min(all_qubits) * CELL_H + CELL_H // 2`
   - Compute `y_max = max(all_qubits) * CELL_H + CELL_H // 2`
   - Draw vertical line: `canvas[y_min:y_max + 1, gx] = CTRL_COLOR`
4. Gate blocks (already done in plan 01 -- ensure this runs AFTER control lines so gates overlay)
5. **NEW: Control dots** -- After gate blocks (rendered last so always visible). For each gate with non-empty `controls`:
   - For each control qubit index in `gate['controls']`:
     - `cy = ctrl * CELL_H + CELL_H // 2`
     - `cx = gate['layer'] * CELL_W`
     - `canvas[cy, cx] = CTRL_COLOR`

**CRITICAL:** The existing plan 01 code likely has control line and dot rendering as placeholders or comments. Fill in those sections. If plan 01 already includes the code structure, ensure the rendering order is correct: wires -> control lines -> gate blocks -> control dots.

Ensure `CTRL_COLOR = (255, 255, 100)` is already defined as a module-level constant (should be from plan 01).
  </action>
  <verify>
Run: `python3 -c "from quantum_language.draw import render, CTRL_COLOR; print(f'CTRL_COLOR={CTRL_COLOR}')"` -- should print (255, 255, 100).
  </verify>
  <done>
render() draws vertical control lines spanning all involved qubits, control dots at each control qubit, in correct rendering order (lines before gates, dots after gates).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add multi-qubit gate rendering tests</name>
  <files>tests/test_draw_render.py</files>
  <action>
Add the following tests to `tests/test_draw_render.py` (append to existing test file from plan 01):

**Use the existing `make_draw_data` helper from plan 01.**

1. `test_cnot_control_line` -- Place a CNOT: X gate (type=0) at layer=0, target=2, controls=[0]. With CELL_H=3, control qubit 0 has wire center at y=1, target qubit 2 at y=7. Verify vertical control line: pixels at (x=0, y=1), (x=0, y=2), ..., (x=0, y=7) should be CTRL_COLOR. Use `np.array(img)` to check column pixels.

2. `test_cnot_control_dot` -- Same CNOT as above. Verify control dot at (x=0, y=1) equals CTRL_COLOR. The target qubit should have the X gate block (type=0 color), not a control dot.

3. `test_ccx_two_controls` -- Place CCX: X gate at layer=0, target=2, controls=[0, 1]. Verify control line spans from qubit 0 (y=1) to qubit 2 (y=7). Verify control dots at both qubit 0 (y=1) and qubit 1 (y=4).

4. `test_control_line_color_distinct_from_wire` -- Verify that CTRL_COLOR != WIRE_COLOR (import both constants and assert inequality).

5. `test_rendering_order_gate_over_control_line` -- Place CNOT: X gate at layer=0, target=1, controls=[0]. Check that the gate block pixels at target qubit (gy=CELL_H*1, gx=0, 2x2 block) show gate color (not control line color). This confirms gates render after control lines.

6. `test_rendering_order_dot_over_gate` -- Place a controlled gate where control qubit has same position as another gate. Verify control dot pixel is CTRL_COLOR even if a gate block overlaps. (Simpler: just verify the control dot position has CTRL_COLOR for a standard CNOT.)

7. `test_mcx_multiple_controls` -- Place gate with type=0, layer=0, target=4, controls=[0, 1, 3]. Verify control line spans from qubit 0 to qubit 4. Verify control dots at qubits 0, 1, and 3 (but NOT qubit 2, which is an intermediate qubit with no control).
  </action>
  <verify>
Run: `cd /path/to/project && python3 -m pytest tests/test_draw_render.py -v` -- all tests (plan 01 + plan 02) should pass.
  </verify>
  <done>
7 new tests pass covering CNOT control line, control dot, CCX with two controls, color distinction, rendering order (gate over line, dot over gate), and MCX with multiple controls. All plan 01 tests still pass.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_draw_render.py -v` -- all tests pass (plan 01 + plan 02)
2. Manual: render a CNOT circuit and save to PNG:
   ```python
   from quantum_language.draw import render
   data = {'num_layers': 3, 'num_qubits': 3, 'gates': [
       {'layer': 0, 'target': 0, 'type': 4, 'angle': 0.0, 'controls': []},
       {'layer': 1, 'target': 2, 'type': 0, 'angle': 0.0, 'controls': [0]},
       {'layer': 2, 'target': 2, 'type': 0, 'angle': 0.0, 'controls': [0, 1]},
   ], 'qubit_map': {}}
   img = render(data)
   img.save('/tmp/test_multi_qubit.png')
   ```
   Open PNG -- should show vertical yellow lines connecting control dots to target gate blocks.
</verification>

<success_criteria>
- Vertical control lines rendered between control and target qubits
- Control dots visible as bright pixels at control positions
- CTRL_COLOR distinct from WIRE_COLOR
- Rendering order correct (control lines under gates, control dots on top)
- All 15 tests pass (8 from plan 01 + 7 new)
</success_criteria>

<output>
After completion, create `.planning/phases/46-core-renderer/46-02-SUMMARY.md`
</output>

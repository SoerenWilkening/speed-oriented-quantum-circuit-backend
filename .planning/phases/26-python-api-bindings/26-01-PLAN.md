---
phase: 26-python-api-bindings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_language/openqasm.pxd
  - src/quantum_language/openqasm.pyx
  - src/quantum_language/__init__.py
  - setup.py
autonomous: true

must_haves:
  truths:
    - "openqasm.pxd declares circuit_to_qasm_string with correct C signature"
    - "openqasm.pyx wraps C function with try-finally memory safety"
    - "ql.to_openqasm() is callable at package level"
    - "pip install -e .[verification] installs Qiskit"
  artifacts:
    - path: "src/quantum_language/openqasm.pxd"
      provides: "C function declaration for circuit_to_qasm_string"
      contains: "circuit_to_qasm_string"
    - path: "src/quantum_language/openqasm.pyx"
      provides: "Memory-safe Python wrapper for OpenQASM export"
      contains: "def to_openqasm"
    - path: "src/quantum_language/__init__.py"
      provides: "Package-level re-export of to_openqasm"
      contains: "to_openqasm"
    - path: "setup.py"
      provides: "Optional verification dependencies"
      contains: "extras_require"
  key_links:
    - from: "src/quantum_language/openqasm.pyx"
      to: "src/quantum_language/_core.pyx"
      via: "accessor functions _get_circuit, _get_circuit_initialized"
      pattern: "_get_circuit"
    - from: "src/quantum_language/__init__.py"
      to: "src/quantum_language/openqasm.pyx"
      via: "import re-export"
      pattern: "from .openqasm import to_openqasm"
---

<objective>
Create the Cython wrapper for OpenQASM 3.0 export and wire it into the package API.

Purpose: Enable users to call `ql.to_openqasm()` to get a complete OpenQASM 3.0 string from their current circuit, with proper C memory management and clean error handling.
Output: openqasm.pxd, openqasm.pyx, updated __init__.py, updated setup.py with extras_require
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-python-api-bindings/26-CONTEXT.md
@.planning/phases/26-python-api-bindings/26-RESEARCH.md
@src/quantum_language/_core.pxd
@src/quantum_language/_core.pyx
@src/quantum_language/__init__.py
@setup.py
@c_backend/include/circuit_output.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create openqasm.pxd and openqasm.pyx Cython wrapper</name>
  <files>src/quantum_language/openqasm.pxd, src/quantum_language/openqasm.pyx</files>
  <action>
  Create `src/quantum_language/openqasm.pxd` with C declarations:
  - Import `circuit_t` from QPU.h (use `cdef extern from "QPU.h"` with `ctypedef struct circuit_t: pass`)
  - Declare `circuit_to_qasm_string` from `circuit_output.h`: `char *circuit_to_qasm_string(circuit_t *circ)`

  Create `src/quantum_language/openqasm.pyx` with the `to_openqasm()` function:
  - Import `free` from `libc.stdlib`
  - Import `_get_circuit` and `_get_circuit_initialized` from `._core` (Python-level import, not cimport — these are def functions)
  - Import `circuit_t` and `circuit_to_qasm_string` via cimport from the .pxd file or inline cdef extern
  - Function signature: `def to_openqasm():`
  - Check `_get_circuit_initialized()` first — raise `RuntimeError("No circuit initialized")` if False
  - Get circuit pointer: `cdef circuit_t* circ = <circuit_t*><unsigned long long>_get_circuit()` (cast from Python int returned by accessor)
  - Declare `cdef char* c_str = NULL`
  - In try block: call `c_str = circuit_to_qasm_string(circ)`, check NULL → raise `RuntimeError("Failed to export circuit to OpenQASM")`, then `return c_str.decode('utf-8')`
  - In finally block: `if c_str != NULL: free(c_str)`
  - Add docstring: "Export current circuit to OpenQASM 3.0 string." with Returns/Raises sections

  IMPORTANT: The _get_circuit() accessor returns `<unsigned long long>_circuit` (a Python int). The cast chain in openqasm.pyx must be `<circuit_t*><unsigned long long>` to convert Python int back to C pointer. This matches the existing pattern used in other modules.
  </action>
  <verify>Files exist with correct content: `ls src/quantum_language/openqasm.pxd src/quantum_language/openqasm.pyx`</verify>
  <done>openqasm.pxd declares circuit_to_qasm_string; openqasm.pyx has to_openqasm() with try-finally memory safety, NULL check, and proper pointer casting</done>
</task>

<task type="auto">
  <name>Task 2: Wire to_openqasm into package API and add extras_require</name>
  <files>src/quantum_language/__init__.py, setup.py</files>
  <action>
  In `src/quantum_language/__init__.py`:
  - Add `from .openqasm import to_openqasm` in the imports section (after the existing `from .qint_mod import qint_mod` line)
  - Add `"to_openqasm"` to the `__all__` list, in a new comment group `# Export` between `# Constants` and `# State`

  In `setup.py`:
  - Add `extras_require` parameter to the `setup()` call:
    ```python
    extras_require={
        "verification": ["qiskit>=1.0"],
    },
    ```
  - Place it after `package_data` and before `python_requires`
  - No changes to Extension discovery needed — the existing glob pattern in setup.py already auto-discovers all .pyx files in src/quantum_language/
  </action>
  <verify>
  `grep "to_openqasm" src/quantum_language/__init__.py` shows both import and __all__ entry.
  `grep "extras_require" setup.py` shows verification extras.
  </verify>
  <done>to_openqasm is importable as ql.to_openqasm(); setup.py has extras_require with verification key containing qiskit>=1.0</done>
</task>

</tasks>

<verification>
1. `ls src/quantum_language/openqasm.pxd src/quantum_language/openqasm.pyx` — both files exist
2. `grep "circuit_to_qasm_string" src/quantum_language/openqasm.pxd` — C declaration present
3. `grep "def to_openqasm" src/quantum_language/openqasm.pyx` — wrapper function defined
4. `grep "finally" src/quantum_language/openqasm.pyx` — memory safety pattern present
5. `grep "to_openqasm" src/quantum_language/__init__.py` — re-exported at package level
6. `grep "extras_require" setup.py` — optional deps configured
</verification>

<success_criteria>
- openqasm.pxd correctly declares circuit_to_qasm_string from circuit_output.h
- openqasm.pyx implements to_openqasm() with try-finally, NULL check, proper casting
- __init__.py imports and re-exports to_openqasm in __all__
- setup.py has extras_require with verification key
</success_criteria>

<output>
After completion, create `.planning/phases/26-python-api-bindings/26-01-SUMMARY.md`
</output>

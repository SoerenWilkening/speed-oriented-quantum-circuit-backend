---
phase: 26-python-api-bindings
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - tests/python/test_openqasm_export.py
autonomous: true

must_haves:
  truths:
    - "ql.to_openqasm() returns valid OpenQASM 3.0 string for a circuit with gates"
    - "ql.to_openqasm() raises RuntimeError when no circuit is initialized"
    - "The returned string contains OPENQASM 3.0 header, qubit declarations, and gate operations"
    - "Package compiles successfully with new openqasm module"
  artifacts:
    - path: "tests/python/test_openqasm_export.py"
      provides: "Test coverage for to_openqasm() wrapper"
      contains: "test_openqasm"
    - path: "src/quantum_language/openqasm.cpython-*"
      provides: "Compiled Cython extension"
  key_links:
    - from: "tests/python/test_openqasm_export.py"
      to: "src/quantum_language/__init__.py"
      via: "import quantum_language as ql; ql.to_openqasm()"
      pattern: "ql.to_openqasm"
---

<objective>
Build the package with the new openqasm module and verify the full pipeline works end-to-end with tests.

Purpose: Confirm that the Cython wrapper compiles, links correctly to the C backend, and produces valid OpenQASM output from Python.
Output: Compiled extension, passing test suite
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-python-api-bindings/26-01-SUMMARY.md
@src/quantum_language/openqasm.pxd
@src/quantum_language/openqasm.pyx
@src/quantum_language/__init__.py
@tests/python/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build package and verify compilation</name>
  <files></files>
  <action>
  Run `python setup.py build_ext --inplace` from the project root to compile all Cython extensions including the new openqasm module.

  If compilation fails:
  - Check that openqasm.pxd has correct `cdef extern from` blocks matching the C header signatures
  - Verify the circuit_t typedef matches between openqasm.pxd and _core.pxd (both use opaque `pass` struct)
  - Check for import/cimport issues between openqasm.pyx and _core module
  - Fix any issues and rebuild

  After successful build, verify:
  - `ls src/quantum_language/openqasm.cpython-*.so` shows compiled extension
  - `python -c "from quantum_language.openqasm import to_openqasm; print('OK')"` imports successfully
  - `python -c "import quantum_language as ql; print(hasattr(ql, 'to_openqasm'))"` prints True
  </action>
  <verify>
  `python -c "import quantum_language as ql; print(hasattr(ql, 'to_openqasm'))"` prints True
  </verify>
  <done>Package compiles with openqasm module; to_openqasm is accessible via ql.to_openqasm</done>
</task>

<task type="auto">
  <name>Task 2: Create and run tests for to_openqasm()</name>
  <files>tests/python/test_openqasm_export.py</files>
  <action>
  Create `tests/python/test_openqasm_export.py` with the following tests using the existing `clean_circuit` fixture from conftest.py:

  1. `test_to_openqasm_returns_string(clean_circuit)` — Create a `ql.qint(5, width=4)`, call `ql.to_openqasm()`, assert result is `str` and `len(result) > 0`

  2. `test_to_openqasm_has_header(clean_circuit)` — Create a qint, call `ql.to_openqasm()`, assert `"OPENQASM 3.0" in qasm` and `'include "stdgates.inc"' in qasm`

  3. `test_to_openqasm_has_qubit_declaration(clean_circuit)` — Create `ql.qint(5, width=4)`, call `ql.to_openqasm()`, assert `"qubit[" in qasm` (qubit declaration present, don't hardcode exact count since ancilla may vary)

  4. `test_to_openqasm_has_gates(clean_circuit)` — Create `ql.qint(5, width=4)` (binary 0101 → X gates on bits 0 and 2), call `ql.to_openqasm()`, assert `"x " in qasm` (X gates present for classical initialization)

  5. `test_to_openqasm_no_circuit_raises()` — WITHOUT clean_circuit fixture (no circuit initialized), call `ql.to_openqasm()` inside `pytest.raises(RuntimeError, match="No circuit initialized")`

  6. `test_to_openqasm_in_all()` — Assert `"to_openqasm" in ql.__all__`

  Import `quantum_language as ql` and `pytest` at the top. Use the standard project test patterns from existing test files.

  Run tests: `pytest tests/python/test_openqasm_export.py -v`

  If any test fails, diagnose and fix either the test or the source code (openqasm.pyx/__init__.py).
  Also run the full existing test suite to check for regressions: `pytest tests/python/ -v --timeout=60`
  </action>
  <verify>
  `pytest tests/python/test_openqasm_export.py -v` — all 6 tests pass
  `pytest tests/python/ -v --timeout=60` — no regressions in existing tests
  </verify>
  <done>All 6 OpenQASM export tests pass; no regressions in existing test suite</done>
</task>

</tasks>

<verification>
1. `python setup.py build_ext --inplace` completes without errors
2. `python -c "import quantum_language as ql; print(ql.to_openqasm.__doc__)"` shows docstring
3. `pytest tests/python/test_openqasm_export.py -v` — all tests pass
4. `pytest tests/python/ -v --timeout=60` — no regressions
</verification>

<success_criteria>
- Package compiles with new openqasm extension module
- ql.to_openqasm() returns valid OpenQASM 3.0 string for circuits with gates
- ql.to_openqasm() raises RuntimeError when no circuit is initialized
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-python-api-bindings/26-02-SUMMARY.md`
</output>

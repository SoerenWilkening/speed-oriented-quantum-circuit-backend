---
phase: 118-nested-with-block-rewrite
plan: 02
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - tests/python/test_nested_with_blocks.py
autonomous: true
requirements: [CTRL-01, CTRL-05]

must_haves:
  truths:
    - "3-level nested with-blocks produce triply-controlled operations (3 conditions AND-composed into 2 AND-ancillas)"
    - "4+ level nesting produces correct results (each level adds one AND-ancilla)"
    - "3-level nesting with mixed True/False conditions at each depth gives correct values"
    - "3-level nesting with arithmetic at outer, middle, and inner levels gives correct cumulative result"
    - "Full test suite passes with zero regressions from Phase 118 changes"
  artifacts:
    - path: "tests/python/test_nested_with_blocks.py"
      provides: "3+ level nesting tests and regression verification"
      contains: "TestThreeLevelNesting"
  key_links:
    - from: "tests/python/test_nested_with_blocks.py"
      to: "src/quantum_language/qint.pyx __enter__/__exit__"
      via: "3-level nested with blocks exercise AND-composition chain"
      pattern: "with c1.*with c2.*with c3"
---

<objective>
Add 3+ level nesting tests that verify AND-composition chains correctly at arbitrary depth, plus run full regression to confirm zero breakage from Phase 118 changes.

Purpose: CTRL-01 requires "arbitrary depth" nesting. Plan 01 verified 2-level; this plan pushes to 3-4+ levels and confirms the linear AND-chain works correctly. Also provides full regression confidence (CTRL-05).

Output: Extended test suite with 3-level and 4-level tests, full regression verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-nested-with-block-rewrite/118-CONTEXT.md
@.planning/phases/118-nested-with-block-rewrite/118-RESEARCH.md
@.planning/phases/118-nested-with-block-rewrite/118-01-SUMMARY.md

<interfaces>
<!-- Qubit budget reference from research. Executor must stay under 17 qubits. -->

Qubit budgets for 3+ level tests:
| Scenario | Components | Total Qubits |
|----------|-----------|--------------|
| 3-level, qbool(T/F) + 2-bit result | 3 conds + 2 result + 2 AND-ancillas = 7 | 7 |
| 4-level, qbool(T/F) + 2-bit result | 4 conds + 2 result + 3 AND-ancillas = 9 | 9 |
| 5-level, qbool(T/F) + 2-bit result | 5 conds + 2 result + 4 AND-ancillas = 11 | 11 |

All safely under 17-qubit limit.

From tests/python/test_nested_with_blocks.py:
```python
def _simulate_and_extract(qasm_str, num_qubits, result_start, result_width):
    """Simulate QASM and extract integer from result register."""
    # Already exists, reuse for all new tests

def _get_num_qubits(qasm_str):
    """Extract number of qubits from OpenQASM string."""
    # Already exists, reuse for all new tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3+ level nesting tests</name>
  <files>tests/python/test_nested_with_blocks.py</files>
  <action>
Add a new `TestThreeLevelNesting` class (or add to existing class -- Claude's discretion) with the following tests. Use `ql.qbool(True/False)` for conditions and `ql.qint(0, width=2)` for results (2-bit to stay under 17 qubits). Follow existing test patterns: `gc.collect()`, `ql.circuit()`, `_keepalive`, `_simulate_and_extract`.

1. **test_three_level_all_true**: 3 nested with-blocks, all True. Outer: result += 1, inner: nothing, innermost: result += 2. Assert result == 3 (1+2). Qubits: 3 conds + 2 result + 2 AND = 7.

2. **test_three_level_outer_false**: c1=False, c2=True, c3=True. Outer blocks everything. Assert result == 0.

3. **test_three_level_middle_false**: c1=True, c2=False, c3=True. Outer arithmetic executes, middle and inner blocked. With outer: result += 1, with c2: with c3: result += 2. Assert result == 1.

4. **test_three_level_inner_false**: c1=True, c2=True, c3=False. Outer and middle execute, innermost blocked. With c1: result += 1, with c2: result += 1, with c3: result += 1. Assert result == 2 (outer + middle).

5. **test_three_level_arithmetic_each_depth**: c1=True, c2=True, c3=True. Add 1 at outer, add 1 at middle (inside c2 but outside c3), add 1 at innermost. Assert result == 3. Use width=2 (max value 3 fits in 2 bits).

6. **test_four_level_smoke**: 4 nested with-blocks, all True. Add 1 at deepest level only. Assert result == 1. Qubits: 4 conds + 2 result + 3 AND = 9.

7. **test_four_level_mixed**: c1=True, c2=True, c3=True, c4=False. Result += 1 at deepest level. Assert result == 0 (deepest blocked).

8. **test_three_level_all_false**: All three False. Assert result == 0.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/python/test_nested_with_blocks.py -v --timeout=120 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 8 new tests pass
    - 3-level nesting produces correct AND-chained results
    - 4-level nesting produces correct AND-chained results
    - Mixed True/False at various depths gives correct values
    - All qubit counts are under 17
  </done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification</name>
  <files></files>
  <action>
Run targeted regression tests to verify Phase 118 changes do not break existing functionality:

1. Run the nested with-block tests: `pytest tests/python/test_nested_with_blocks.py -v`
2. Run control stack tests: `pytest tests/python/test_control_stack.py -v`
3. Run compile tests: `pytest tests/python/test_compile.py -v --timeout=120` (14-15 pre-existing xfails are expected)
4. Run oracle tests: `pytest tests/python/test_oracle.py -v --timeout=120`
5. Run arithmetic tests: `pytest tests/python/test_arithmetic*.py -v --timeout=120`
6. Run bitwise tests: `pytest tests/python/test_bitwise*.py -v --timeout=120`
7. Run the single-level with-block baseline tests: `pytest tests/python/test_nested_with_blocks.py::TestSingleLevelConditional -v`

For each batch, confirm zero new failures. The 14-15 pre-existing xfails in test_compile.py are known (documented in STATE.md blockers) and should remain unchanged.

If any test fails that is NOT a pre-existing failure, investigate and determine if it is caused by Phase 118 changes. If so, fix the root cause in qint.pyx/qint_preprocessed.pyx.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/python/test_nested_with_blocks.py tests/python/test_control_stack.py tests/python/test_oracle.py -v --timeout=120 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Zero new test failures across all test batches
    - Pre-existing xfails in test_compile.py remain unchanged
    - All TestSingleLevelConditional tests pass (CTRL-05)
    - All TestNestedWithBlocks tests pass (CTRL-01)
    - All TestThreeLevelNesting tests pass (CTRL-01 at arbitrary depth)
    - Control stack tests pass (Phase 117 infrastructure intact)
    - Oracle tests pass (save/restore not broken)
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/python/test_nested_with_blocks.py -v` -- all tests pass (2-level, 3-level, 4-level, invert, width validation)
2. `pytest tests/python/test_control_stack.py -v` -- all 21 tests pass
3. `pytest tests/python/test_oracle.py -v --timeout=120` -- no new failures
4. `pytest tests/python/test_compile.py -v --timeout=120` -- only pre-existing xfails
</verification>

<success_criteria>
- 3-level nesting verified with all True, mixed conditions, and arithmetic at each depth
- 4-level nesting verified with all True and mixed conditions
- Full regression across control stack, oracle, compile, arithmetic, and bitwise tests shows zero new failures
- CTRL-01 confirmed at arbitrary depth (2, 3, 4 levels)
- CTRL-05 confirmed via TestSingleLevelConditional and broader regression
</success_criteria>

<output>
After completion, create `.planning/phases/118-nested-with-block-rewrite/118-02-SUMMARY.md`
</output>

---
phase: 91-arithmetic-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: [91-01, 91-02]
files_modified:
  - tests/test_div.py
  - tests/test_mod.py
  - tests/test_toffoli_division.py
  - tests/test_modular.py
  - benchmarks/divmod_benchmark.md
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "Division produces correct results for ALL input combinations at widths 2-4 (no xfail cases remain)"
    - "Modulo produces correct results for ALL input combinations at widths 2-4 (no xfail cases remain)"
    - "Modular arithmetic (add, sub, mul) produces correct results without xfail markers"
    - "circuit_stats()['current_in_use'] is stable across repeated division operations"
    - "All previously-passing arithmetic tests continue to pass (zero regressions)"
    - "Benchmark results comparing old QFT vs new classical-gate divmod are saved"
  artifacts:
    - path: "tests/test_div.py"
      provides: "Updated division tests with no xfail markers for MSB leak"
    - path: "tests/test_mod.py"
      provides: "Updated modulo tests with no xfail markers for MSB leak"
    - path: "tests/test_toffoli_division.py"
      provides: "Updated Toffoli division tests with no xfail markers"
    - path: "tests/test_modular.py"
      provides: "Updated modular arithmetic tests with no xfail markers"
    - path: "benchmarks/divmod_benchmark.md"
      provides: "Gate count, depth, ancilla, T-gate comparison between old QFT and new divmod"
  key_links:
    - from: "tests/test_div.py"
      to: "src/quantum_language/qint_division.pxi"
      via: "qint.__floordiv__ calling C-level divmod"
      pattern: "a // divisor"
    - from: "tests/test_modular.py"
      to: "src/quantum_language/qint_mod.pyx"
      via: "qint_mod.__add__ calling C-level mod_reduce"
      pattern: "qint_mod.*\\+"
---

<objective>
Verify all arithmetic bug fixes, update test suites, run benchmarks, and confirm zero regressions.

Purpose: Ensure BUG-DIV-02, BUG-QFT-DIV, and BUG-MOD-REDUCE are fully fixed by running exhaustive tests, removing all xfail markers, verifying circuit_stats stability, benchmarking old vs new, and confirming zero regressions.

Output: All tests passing without xfail markers. Benchmark results saved. Regression suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/91-arithmetic-bug-fixes/91-RESEARCH.md
@.planning/phases/91-arithmetic-bug-fixes/91-CONTEXT.md
@.planning/phases/91-arithmetic-bug-fixes/91-01-SUMMARY.md
@.planning/phases/91-arithmetic-bug-fixes/91-02-SUMMARY.md
@tests/test_div.py
@tests/test_mod.py
@tests/test_toffoli_division.py
@tests/test_modular.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test suites and verify correctness</name>
  <files>
    tests/test_div.py
    tests/test_mod.py
    tests/test_toffoli_division.py
    tests/test_modular.py
  </files>
  <action>
Update all division and modular arithmetic test files to remove xfail markers and verify the bugs are fixed.

**tests/test_div.py:**
- Remove the `KNOWN_DIV_MSB_LEAK` set entirely.
- Remove the `_mark_msb_leak_cases` function.
- Update `@pytest.mark.parametrize` to pass raw cases without xfail wrapping.
- Update `_run_div` to use MPS simulator with transpilation for large circuits (width 4 may exceed statevector limits with the new C-level implementation).
- Add a new test `test_div_circuit_stats_stable` that:
  1. Creates a circuit
  2. Performs division: `a = qint(7, width=4); q = a // 3`
  3. Records `circuit_stats()['current_in_use']`
  4. Performs another division: `b = qint(5, width=4); r = b // 2`
  5. Asserts that `current_in_use` growth equals only the new registers (not leaked ancillae)
- All exhaustive tests (widths 1-3) and sampled tests (width 4) must pass.

**tests/test_mod.py:**
- Remove the `KNOWN_MOD_MSB_LEAK` set entirely.
- Remove the `_mark_msb_leak_cases` function.
- Update parametrize to pass raw cases.
- Update `_run_mod` to use MPS simulator with transpilation if needed.
- All tests must pass.

**tests/test_toffoli_division.py:**
- Remove `KNOWN_TOFFOLI_DIV_FAILURES`, `KNOWN_TOFFOLI_MOD_FAILURES_W2`, `KNOWN_TOFFOLI_QDIV_FAILURES`, `KNOWN_TOFFOLI_QMOD_FAILURES` sets.
- Remove all `_mark_*_failures` functions.
- Update parametrize to pass raw cases.
- All tests must pass (classical div, classical mod, quantum div, quantum mod, gate purity).
- Note: since we now always use Toffoli gates (C-level), the `_enable_toffoli()` calls may no longer be needed -- but keep them for explicit mode verification.

**tests/test_modular.py:**
- Remove all `_is_known_*_failure` functions.
- Remove all `pytest.xfail` calls from test functions.
- Update test functions to assert directly without xfail wrapping.
- Update the `_calibrate_position` approach -- with C-level modular reduction, the qubit layout should be deterministic, so calibration should work reliably.
- If the extraction position approach is still fragile, switch to using `allocated_start` from the result qint_mod (same pattern as test_toffoli_division.py).

**Run the full arithmetic test suite:**
```bash
pytest tests/test_div.py tests/test_mod.py tests/test_toffoli_division.py tests/test_modular.py tests/test_add.py tests/test_sub.py tests/test_mul.py tests/test_toffoli_addition.py tests/test_toffoli_multiplication.py tests/test_compare.py -v --tb=short
```
All must pass. Any failures must be investigated and fixed (may require returning to Plan 91-01 or 91-02 to fix C code).

**Important constraints:**
- Max 17 qubits for statevector simulation
- Max 4 threads for Qiskit simulation (use `max_parallel_threads=4`)
- Use MPS simulator with transpilation for circuits exceeding 17 qubits
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/test_div.py tests/test_mod.py tests/test_toffoli_division.py tests/test_modular.py -v --tb=short 2>&1 | tail -30</automated>
    <manual>Verify all tests pass, no xfail markers remain for BUG-DIV-02, BUG-QFT-DIV, or BUG-MOD-REDUCE</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    All division, modulo, and modular arithmetic tests pass without xfail markers. circuit_stats stability test passes. Zero KNOWN_*_LEAK or KNOWN_*_FAILURES sets remain in test files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Benchmark and regression verification</name>
  <files>
    benchmarks/divmod_benchmark.md
  </files>
  <action>
Run benchmarks comparing old QFT division vs new classical-gate divmod, and verify zero regressions in the arithmetic test suite.

**Benchmark (QFT vs classical-gate divmod):**
Note: The old QFT division code is being deleted. To benchmark, we need to capture metrics BEFORE deletion. If Plan 91-01 has already deleted the old code, use git to check out the old version temporarily for benchmarking, or document that the benchmark was performed during development.

If old code is still available (git stash/checkout):
- For widths 2, 3, 4:
  1. Generate old QFT division circuit: `ql.circuit(); a = ql.qint(val, width=w); q = a // divisor; qasm = ql.to_openqasm()`
  2. Count gates by type (X, CX, CCX, H, P, CP, etc.)
  3. Measure circuit depth (number of layers)
  4. Count ancilla qubits used
  5. Count T-gates (from Clifford+T decomposition if applicable)
  6. Repeat with new classical-gate divmod

If old code is already deleted:
- Benchmark only the new implementation
- Note in the benchmark file that QFT division was known-broken and metrics are for the replacement only
- Use `ql.circuit_stats()` to capture qubit usage
- Parse QASM output to count gates by type

Save results to `benchmarks/divmod_benchmark.md` with:
- Table comparing metrics per width (gate count, depth, ancilla, T-gate count)
- Note which approach "wins" for each metric
- Date and configuration (Toffoli mode, no CLA override)

**Regression verification:**
Run the full arithmetic-related test suite (not the full project test suite):
```bash
pytest tests/test_add.py tests/test_sub.py tests/test_mul.py tests/test_div.py tests/test_mod.py tests/test_toffoli_addition.py tests/test_toffoli_multiplication.py tests/test_toffoli_division.py tests/test_compare.py tests/test_modular.py tests/test_cla_addition.py tests/test_copy_binops.py -v --tb=short
```

All must pass. If any previously-passing test now fails, that is a regression and must be fixed before marking this task complete.

**Reversibility test:**
For widths 2-3, verify division is reversible:
1. Create circuit, perform division
2. Get QASM output
3. Verify that the circuit can be reversed (all gates are reversible -- Toffoli gates are self-inverse)
4. This is verified implicitly by the CDKM adder's proven reversibility -- document this.
  </action>
  <verify>
    <automated>cd /Users/sorenwilkening/Desktop/Projects/Quantum_Assembly && pytest tests/test_add.py tests/test_sub.py tests/test_mul.py tests/test_div.py tests/test_mod.py tests/test_toffoli_addition.py tests/test_toffoli_multiplication.py tests/test_toffoli_division.py tests/test_compare.py tests/test_modular.py -v --tb=short 2>&1 | tail -20</automated>
    <manual>Verify zero regressions in arithmetic test suite and benchmark file exists</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    Benchmark results saved to benchmarks/divmod_benchmark.md. All arithmetic-related tests pass (including those that passed before Phase 91). Zero regressions confirmed.
  </done>
</task>

</tasks>

<verification>
- [ ] All division tests pass without xfail markers
- [ ] All modulo tests pass without xfail markers
- [ ] All modular arithmetic tests pass without xfail markers
- [ ] circuit_stats stability test passes
- [ ] Benchmark file exists at benchmarks/divmod_benchmark.md
- [ ] Zero regressions in arithmetic test suite
- [ ] Reversibility is documented/verified
</verification>

<success_criteria>
All three bugs (BUG-DIV-02, BUG-QFT-DIV, BUG-MOD-REDUCE) are verified fixed. No xfail markers remain for these bugs. Benchmark results are saved. All previously-passing tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/91-arithmetic-bug-fixes/91-03-SUMMARY.md`
</output>
